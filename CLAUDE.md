# Poker Trainer — Project Context

Этот файл читается Claude Code при каждом запуске. Здесь зафиксировано всё, что нужно знать о проекте прежде чем начинать работу: архитектура, принятые решения, текущее состояние, договорённости. Если что-то менялось — обновляй этот файл, чтобы следующая сессия не дрейфовала.

---

## О проекте

Веб-тренажёр покерных диапазонов и постфлоп-решений. Универсальный — не привязан к конкретному руму или формату, работает с любыми диапазонами в нашем JSON-формате. Целевые игры: **NL Hold'em**, **6-max кэш** и **MTT 8-max**, глубина 100bb.

Конечная цель — собственный инструмент для тренировки и анализа: визуализатор + редактор диапазонов, тренажёр префлопа и постфлопа, в перспективе анализатор сыгранных раздач.

## О пользователе

- **Уровень в программировании:** начинающий. Проект — способ одновременно подтянуть код и выбраться с микролимитов в покере. Объясняй технические решения, не вали в код без пояснений.
- **Язык общения:** русский. Код, имена файлов, переменные — английский (это явно принятое решение).
- **Стиль работы:** обсуждаем дизайн перед написанием кода. Большие шаги декомпозируем на этапы с проверкой. Не делаем нескольких рискованных вещей одновременно.
- **OS:** macOS. Проект лежит в `~/Desktop/Poker_Trainer/`.

---

## Технологический стек

- **Backend:** Python 3, FastAPI. Файлы запускаются как обычно через `uvicorn main:app --reload` или просто `python3 main.py`.
- **Frontend:** чистый HTML + CSS + JS, без фреймворков и без сборщика. Подаётся как статика из `static/` через FastAPI.
- **Persistence:** JSON-файлы в проекте (`stats.json`, `history.json`, `srs_state/*.json`). Никакой БД. *(Track D: решено мигрировать на Supabase Postgres — см. решение #16; ещё не реализовано.)*
- **Диапазоны:** JSON в `data/`. Один файл — один range pack (cash 100bb 6-max, MTT 100bb 8-max и т.д.). Никакого хардкода диапазонов в коде.

---

## Структура директорий

```
Poker_Trainer/
├── main.py                  ← FastAPI app, корневые эндпоинты, подключение роутеров
├── range_engine.py          ← загрузка/нормализация range-файлов
├── drill_engine.py          ← логика префлоп-тренажёра (Drill / Practice Mode)
├── equity.py                ← Монте-Карло equity, точный перебор на ривере (проверен!)
├── decision.py              ← фолд/колл по пот-оддсам
├── postflop_engine.py       ← генерация постфлоп-спотов, типы оппонентов
├── postflop_api.py          ← FastAPI router /api/postflop
├── equity_api.py            ← FastAPI router /api/equity
├── srs.py                   ← SM-2 алгоритм, модель карточки, persistence (см. ниже)
├── srs_api.py               ← FastAPI router /api/srs (Study Mode)
├── srs_fsrs.py              ← FSRS-lite планировщик (py-fsrs), параллельно SM-2, НЕ в srs_api (dev-only: fsrs не в requirements)
├── hh_parser.py             ← парсер hand-history (CoinPoker + PokerStars форматы)
├── data/                    ← range packs (JSON), не в гите если sensitive
│   ├── cash_micro_100bb.json
│   └── gto_100bb_mtt.json
├── srs_state/               ← состояние SRS, по файлу на каждый range pack
│   └── <range_stem>.srs.json
├── parsed/                  ← выход hh_parser (бакеты JSONL по рум/лимит/формат)
├── cards/                   ← SVG-плитки карт (GTOW-стиль, генератор cards/gen_cards.py), 4-цвет (см. #12 поправка 2026-06-14)
│   ├── heartAce.svg ...     ← 52 лица: <suit><rank>.svg (heart/diamond/club/spade + Ace/King/Queen/Jack/10/9..2); десятка рисуется «T», файл остаётся <suit>10.svg
│   ├── gen_cards.py         ← генератор плиточной колоды (python3 cards/gen_cards.py; TILES_OUT=dir → staging)
│   ├── goldBack.svg         ← рубашка оппонентов (чёрный+золото, ornate); рисует .mini-back и .card-back (2026-06-03)
│   ├── goldBack.gen.py       ← генератор goldBack.svg (воспроизводимый источник рубашки)
│   ├── blueBack.svg         ← старая рубашка из колоды, НЕ используется (заменена goldBack.svg)
│   ├── _backup_realistic_20260614/ ← бэкап прежней реалистичной колоды (saulspatz, до плитки) — откат
│   └── _original/           ← бэкап оригиналов до перекраски (откат в один шаг)
├── static/                  ← фронтенд
│   ├── index.html           ← одна страница, навигация по вкладкам
│   ├── style.css
│   ├── visualizer.js
│   ├── editor.js
│   ├── drill.js
│   ├── postflop.js
│   └── mobile.js            ← мобилка: bottom-sheet настроек + плавающая Deal (Track B.4, под @media ≤768px)
├── stats.json               ← статистика ответов префлоп-тренажёра
├── history.json             ← последние 200 рук с ответами
└── tests/                   ← test_*.py — запускаются как обычные скрипты
```

---

## Модули — назначение

| Файл | Что делает | Стабильность |
|---|---|---|
| `main.py` | Корневые эндпоинты `/api/ranges`, `/api/drill/*`, `/api/stats`, `/api/history`. Включает роутеры. Сервит фронтенд из `static/`. | Стабилен, трогать аккуратно |
| `range_engine.py` | `load_range_file()`. Нормализует ключи `spots` vs `ranges` (legacy совместимость). | Стабилен |
| `drill_engine.py` | Генерация префлоп-спотов (RFI / vs_RFI / vs_3bet), проверка ответа. ГСЧ для микс-частот. | Стабилен |
| `equity.py` | Монте-Карло equity для рука-vs-диапазон. Точный перебор на ривере. **Трижды сверен с equity.poker — расхождения только в подсчёте комбо с блокерами (наша математика строже).** | Закрыт от правок без сильной причины |
| `decision.py` | Фолд/колл по пот-оддсам. | Стабилен |
| `postflop_engine.py` | Постфлоп-споты, типы оппонентов (Fish / Maniac / Regular / GTO). Работает только на флопе пока. | В разработке (Milestone 2) |
| `postflop_api.py` | `/api/postflop/*` | В разработке |
| `equity_api.py` | `/api/equity/*` | Стабилен |
| `srs.py` | Spaced Repetition System. Модель `Card`, SM-2 алгоритм (`update_card`), расписание (`get_due_cards`), persistence, `grade_answer` (объективная оценка → SM-2 рейтинг). 64 юнит-теста. | Новый, стабильный |
| `srs_api.py` | `/api/srs/*` — status / init / next / answer / summary / reset. 18 интеграционных тестов. | Новый, стабильный |
| `srs_fsrs.py` | FSRS-lite планировщик поверх `py-fsrs` (FSRS-6), **параллельно** SM-2, НЕ подключён к `srs_api`. `seed_difficulty` (difficulty из частот стратегии), `replay`/`good_ladder`. Анализ/эксперимент A.4. 45 тестов (`test_srs_fsrs.py`). | Новый, dev-only (fsrs не в requirements) |
| `hh_parser.py` | Парсер раздач из текстовых архивов. CoinPoker + PokerStars, USDT/EUR/USD. Бакетирует по rum/limit/format. | Стабилен |

---

## Формат range-файлов (JSON)

```jsonc
{
  "meta": {
    "game_type":    "MTT",            // "Cash" | "MTT"
    "table_size":   "8max",
    "stack_depth":  "100bb",
    "label":        "GTO 100bb MTT 8-max"
  },
  "config": {
    "positions":       ["UTG", "UTG+1", "MP", "HJ", "CO", "BTN", "SB", "BB"],
    "rfi_positions":   ["UTG", "UTG+1", "MP", "HJ", "CO", "BTN", "SB"],
    "vs_rfi_options":  { "BTN": ["UTG", "MP", "CO"], ... },
    "vs_3bet_options": { "UTG": ["BTN", "SB", "BB"], ... },
    "squeeze_options": { "BB": ["BTN-SB", "CO-BTN"], ... }   // hero: ["<opener>-<caller>", ...]
  },
  "spots": {
    "RFI": {
      "UTG": { "AA": {"open": 1.0}, "A5s": {"open": 0.7}, ... }
      // missing hands = implicit fold (skipped in trainers/SRS)
    },
    "vs_RFI": {
      "SB": {
        "vs_UTG": { "AA": {"3bet": 1.0}, "AKs": {"3bet": 0.6, "call": 0.4}, ... }
      }
    },
    "vs_3bet": { ... },
    "squeeze": {
      "BB": {
        "BTN-SB": { "AKs": {"squeeze": 1.0}, "99": {"squeeze": 0.6, "call": 0.4}, ... }
        // sub-key "<opener>-<caller>" — порядок посадки: опенер первым, коллер вторым
        // разделитель "-" (НЕ "+": позиция может содержать "+", напр. UTG+1 → split сломался бы)
        // squeeze = ровно 1 рейзер (опенер) + >=1 коллер + hero; мульти-коллер позже: "opener-c1-c2"
      }
    }
  }
}
```

**Имена действий зависят от спота:** RFI → `open`, vs_RFI → `call` / `3bet`, squeeze → `call` / `squeeze`, vs_3bet → `call` / `4bet`, vs_4bet → `call` / `allin`, iso → `raise` / `call`. Implicit `fold` дополняется до 1.0 только если total < 1.0. Не выдумывай новые имена действий — фронтенд и оценка завязаны на эти.

Старый ключ `ranges` поддерживается через `range_engine._normalize_range_data` (зеркалится в `spots`). Не вычищай — есть legacy файлы.

**Опциональный блок `ev` (GTO-EV прибыльного действия).** Параллелен `spots`, держится ОТДЕЛЬНО от него — внутрь action-словаря EV не кладём (иначе его подхватят `available_actions`, `calculate_rfi_stats`, `expand_rfi_range`, матрица-подсказка). Структура зеркалит `spots`, значения — EV в bb, per-hand (точные ключи, без нотации):

```jsonc
"ev": {
  "RFI":    { "UTG": { "AA": 2.31, "A5s": 0.42 } },
  "vs_RFI": { "SB":  { "vs_UTG": { "AKs": 1.2 } } },   // на будущее
  "squeeze":{ "BB":  { "BTN-SB": { "AKs": 1.83 } } }   // зеркалит spots.squeeze
}
```

Лукап — `range_engine.get_ev(data, spot, position, hand, villain_position=None)` → `float | None`. `None` = нет блока / нет записи → **EV не показываем** (тренажёр падает на чистое правильно/неправильно). Сейчас заполнен только `RFI`. Показ в Drill (решение #12 ниже): EV только на правильном открытии; правильный фолд и любая ошибка — без EV.

---

## SRS Module — Технический справочник

**Что это.** Spaced Repetition System для тренировки запоминания диапазонов в Learn Mode. Реализует SM-2 алгоритм в стиле Chessable: объективная оценка (попал в стратегию / не попал) + опциональная кнопка «Легко» для известных карточек. Логика и почему выбран этот подход см. в разделе «Ключевые решения» #6–#9.

**Файлы:**
- `srs.py` — модель `Card`, SM-2 алгоритм, persistence. Без зависимостей кроме stdlib.
- `srs_api.py` — FastAPI router, эндпоинты `/api/srs/*`
- `srs_state/<range_stem>.srs.json` — состояние deck per range file
- `test_srs.py` — 64 юнит-теста
- `test_srs_api.py` — 18 интеграционных тестов против реального range-файла

### Модель Card

Одна карточка = одна (рука, hero позиция, спот, villain позиция). Пример: `AKs SB vs_RFI vs UTG`.

```python
@dataclass
class Card:
    # Identity (неизменно за жизнь карточки)
    hand: str                       # "AKs", "T9o", "22"
    position: str                   # hero: "UTG", "MP", "CO", "BTN", "SB"
    spot: str                       # "RFI" | "vs_RFI" | "squeeze" | "vs_3bet"
    villain_position: str = ""      # пустое для RFI; squeeze: пара "<opener>-<caller>"
    correct_strategy: dict[str, float]  # {"open": 0.7, "fold": 0.3}

    # SRS state
    ease_factor: float = 2.5        # стартовая лёгкость
    interval_days: int = 0          # 0 = new, never reviewed
    next_review: str = ""           # ISO date
    last_seen: str = ""

    # Stats
    consecutive_correct: int = 0
    total_seen: int = 0
    total_correct: int = 0
```

`card.card_id` — детерминированный ID для API: `"AA__UTG__RFI"` для RFI или `"AKs__SB__vs_RFI__UTG"` для vs-спотов. Squeeze ложится на vs-форму, villain = пара: `"AKs__BB__squeeze__BTN-SB"` (поля по-прежнему режутся по `__`; внутри пары разделитель `-`, а не `+` — позиция может содержать `+`, напр. `UTG+1`).

### Имена действий по спотам

| Спот | Возможные действия | Пример карточки |
|---|---|---|
| RFI | `open`, `fold` (implicit) | `{"open": 1.0}` |
| vs_RFI | `call`, `3bet`, `fold` | `{"call": 0.6, "3bet": 0.4}` |
| squeeze | `squeeze`, `call`, `fold` | `{"squeeze": 0.6, "call": 0.4}` |
| vs_3bet | `call`, `4bet`, `fold` | `{"call": 0.3, "4bet": 0.7}` |
| vs_4bet | `call`, `allin`, `fold` | `{"allin": 0.8, "call": 0.2}` |
| iso | `raise`, `call`, `fold` | `{"raise": 0.7, "call": 0.3}` |

Implicit `fold` дополняется до 1.0 только когда сумма частот в файле < 1.0. UI-кнопки строятся по типу спота, а не по ключам в `correct_strategy` — иначе для pure-карточек одна из кнопок пропадёт.

### SM-2 алгоритм — поведение по рейтингам

| Рейтинг | Значение | `interval_days` | `ease_factor` | Когда вызывается |
|---|---|---|---|---|
| AGAIN = 1 | Снова | сброс в 1 | −0.20 (минимум 1.3) | Действие НЕ в стратегии |
| HARD = 2 | Тяжело | ×1.2, минимум +1 день | −0.15 | Не используется в Learn UI |
| GOOD = 3 | Хорошо | по graduation, затем ×ease | без изменений | Действие в стратегии, без Easy-флага |
| EASY = 4 | Легко | ×ease × 1.3 | +0.15 | Действие в стратегии + пользователь нажал «Легко» |

**Graduation ladder для GOOD:** `0 → 1 → 3 → 3×ease → ...`. То есть впервые увидел → завтра, второй раз правильно → через 3 дня, третий → через 7-8 дней, дальше геометрически по ease.

**«Learned» = `interval_days >= 21`** — порог попадания в долговременную память (для дашборда).

**Константы в `srs.py`:**
```python
PURE_THRESHOLD = 0.95          # частота выше этого → стратегия pure_<action>
DEFAULT_EASE = 2.5             # стартовая лёгкость новой карточки
MIN_EASE = 1.3                 # пол ease после многих ошибок
NEW_CARDS_PER_DAY = 15         # лимит новых карточек в очереди (Anki-default)
LEARNED_THRESHOLD_DAYS = 21    # порог «долговременной памяти»
```

**Порядок ввода новых карт + лимит (поправка 2026-06-15, P-029).** `get_due_cards` отдаёт новые карты НЕ в порядке колоды (= порядок рук в JSON, пары/топ-suited первыми), а в **стабильном шафле по `md5(card_id)`** — детерминированно, не зависит от JSON-порядка, стабильно между днями. Иначе дневной лимит всегда срезал один и тот же верх диапазона и хоронил маргинальные руки границы. `NEW_CARDS_PER_DAY=15` остаётся дефолтом, но теперь **настраивается через API**: `/api/srs/next` и `/api/srs/answer` принимают `new_limit` (фронт-селектор `10/15/30/50/∞`, `∞`→100000). Лимит — кап показа очереди, НЕ свойство колоды (в `srs_state` не пишется, после init не лочится в отличие от scope).

### grade_answer — мост UI → SM-2

```python
srs.grade_answer(card, user_action: str, marked_easy: bool = False) -> int
```

Логика:
- `user_action` есть в `correct_strategy` с частотой > 0 → `GOOD` (или `EASY` если `marked_easy=True`)
- `user_action` отсутствует или с частотой 0 → `AGAIN` (даже если флаг Easy выставлен — флаг игнорируется на неправильном ответе)

**Микс-карточки.** Для `A5s UTG = {"open": 0.7, "fold": 0.3}` ОБА действия (open и fold) грейдятся как GOOD. Это сознательное решение (см. #7 в «Ключевых решениях»): знание про микс-природу спота важнее совпадения с ГСЧ-розыгрышем.

### API endpoints

| Метод | Путь | Назначение |
|---|---|---|
| GET | `/api/srs/status?file=...` | Инициализирован deck? Counts (total/new/due_today/young/learned) |
| POST | `/api/srs/init` | Body: `{file, scope?, force?}`. Создать deck из range-файла. 409 если deck уже есть и `force` не выставлен |
| GET | `/api/srs/next?file=...` | Следующая карточка БЕЗ `correct_strategy` в payload (не спойлерим ответ). Опц. `&new_limit=N` — кап новых карт/день (дефолт `NEW_CARDS_PER_DAY`) |
| POST | `/api/srs/answer` | Body: `{file, card_id, user_action?, marked_easy?, reveal?, new_limit?}`. `reveal=true` («Показать ответ») → грейд AGAIN независимо от action, `grading.revealed=true`. `new_limit?` — кап новых карт/день для очереди следующей карточки. Вернёт `{grading, card (с раскрытой стратегией), next, queue_size}` |
| GET | `/api/srs/summary?file=...` | Только counts (то же что в status, но без поля `initialized`) |
| POST | `/api/srs/reset?file=...` | Стереть SRS-state файл для этого диапазона |

**Параметр `scope` для init:** ограничивает типы спотов в deck. Например `["RFI"]` создаст deck только из RFI-карточек (для MVP/Phase 1). По умолчанию — все три спота.

### Типичный flow для фронтенда

```
1. GET  /api/srs/status?file=...
   → если initialized=false, показать кнопку "Начать обучение"
2. POST /api/srs/init  (один раз)
3. GET  /api/srs/next?file=...
   → показать карточку, кнопки действий по типу спота + «Показать ответ»
4. POST /api/srs/answer
   → action-кнопка: {user_action}; «Показать ответ»: {reveal:true}
   → показать grading (correct / wrong / revealed), раскрыть correct_strategy,
     показать кнопку "Дальше"
5. Повторять 3-4 до queue_size=0 на сегодня
```

### Перед изменением SRS-кода

Обязательный прогон:
```bash
python3 test_srs.py && python3 test_srs_api.py
```
Все 82 теста (64 `test_srs.py` + 18 `test_srs_api.py`) должны быть зелёными. Особенно важно после правок SM-2-математики или классификации pure/mixed — там тонкие edge-кейсы (graduation ladder, ease clamping, near-pure threshold, стабильный шафл новых карт).

---

## Этапы (Milestones) — статус на сегодня

### ✅ Milestone 1 — Визуализатор + Редактор диапазонов
Полностью готов. Внутри:
- Матрица 13×13 для просмотра любого диапазона
- Три типа спотов: RFI / vs_RFI / vs_3bet
- ГСЧ для микс-частот — руки «не сегодня» показаны диагональной штриховкой
- Редактор через клик по матрице с выбором частот (аналог Flopzilla)
- Селекторы Cash/MTT и глубины стека

### ✅ Milestone 2 — Префлоп-тренажёр (Drill / Practice Mode)
Полностью готов. Внутри:
- Стол с участниками раздачи и двумя картами в стиле Wizard/freenetrange
- Три режима: RFI / vs_RFI / vs_3bet
- Опциональный таймер с SVG-кольцом, краснеющим к концу
- Статистика по спотам и позициям в `stats.json`
- Панель с табами Stats / History — видны все ошибки и верные ответы (`history.json`)
- Упрощённый EV (по частоте в диапазоне, без солвера)

### 🔄 Milestone 2 (часть 2) — Постфлоп-тренажёр
В процессе. **Готово:** `equity.py` (проверен), `decision.py`, типы оппонентов (Fish / Maniac / Regular / GTO), Breakdown в стиле equity.poker (что из чего состоит в диапазоне оппонента после ответа). **Работает только на флопе.** **Не готово:** терн, ривер, Vulnerability, привязка размеров ставок к типу оппонента, замена DRAFT-диапазонов оппонентов на реальные из базы рук.

### 🔄 Milestone 2 (часть 3) — Study Mode (SRS)
В процессе. **Готово (backend):** `srs.py` + `srs_api.py`, SM-2 алгоритм, Chessable-style оценка, persistence per range file, 64 юнит + 18 интеграционных тестов. **Не готово (frontend):** новая вкладка Learn в `static/index.html`, JS-страница, переименование текущей Drill → Practice.

### 🚫 Milestone 3 — Анализатор раздач
Не начат. Парсер истории (`hh_parser.py`) готов как фундамент — он распарсил 109k+ рук из текстовой базы CoinPoker/PokerStars в бакетированный JSONL. Реплеер раздач — отложенная часть этой вехи, делаем когда дойдём.

---

## Ключевые решения — НЕ передоговариваться без явной причины

1. **Универсальность.** Тренажёр не привязан к конкретному руму или формату. Работает с любым диапазоном в нашем JSON-формате.

2. **Префлоп — теоретические (no-ante) диапазоны.** Не выводим их из базы рук. Берём из солверов/готовых ренджей.

3. **Постфлоп — диапазоны оппонентов из реальной базы.** Используем 3 млн рук с CoinPoker (ante-game) с допущением, что постфлоп-поведение типа игрока слабо зависит от ширины префлопа. Это не идеально, но рабочее приближение.

4. **Терн/ривер — сквозная раздача с сужением, не независимые улицы.** Содержательно сложнее, но честнее.

5. **Equity-движок свой (`equity.py`), не внешний.** Сверен с equity.poker, никаких внешних солверов не подключаем — это лишняя зависимость.

6. **SRS — Chessable/Duolingo-стиль, не Anki-стиль.** Объективная бинарная оценка (попал в стратегию → Good, не попал → Again). Кнопки Hard и Again-как-самооценка убраны: ввод у нас принудительный (клик действия), система объективно знает результат, заставлять пользователя самооценивать поверх — лишний шаг. Это решение зафиксировано после обсуждения с обоснованием — переоткрывать без серьёзной причины.

    **Поправка от 2026-06-01 (по решению пользователя): кнопка Easy убрана из UI, добавлена третья кнопка «Показать ответ».** Причина: принудительный бинарный ввод (open/fold) ломает сигнал SRS на угадывании — на споте с единственным верным действием случайный клик даёт 50% «правильно», карточка получает GOOD и растягивает интервал, хотя её не выучили. Третья кнопка «Показать ответ» — честный выход из лотереи: пользователь, который не знает ответа, жмёт её вместо того чтобы угадывать. Грейдинг: open/fold в стратегии → GOOD; open/fold мимо → AGAIN; «Показать ответ» → раскрытие + AGAIN (как незнание). «Не знаю» и «ошибся» для алгоритма пока одинаковы (оба AGAIN) — развести можно на A.4. Название именно «Показать ответ» (не «не уверен»): описывает действие/состояние памяти, а не эмоцию — «не уверен» провоцирует тревожную перестраховку и сбивает интервалы. Кнопка равного веса с action-кнопками (не стыдим честность), но нейтрального (не-стратегического) цвета `btn-reveal`. Easy убрана только из UI: backend-примитив EASY-рейтинга, `srs.upgrade_good_to_easy` и эндпоинт `/api/srs/upgrade_easy` оставлены нетронутыми (часть проверенного SM-2-ядра, 81 тест зелёный), просто больше не вызываются из фронта. Реализация: `srs_api.AnswerRequest.reveal: bool`, ветка reveal→AGAIN в `/answer` (+`grading.revealed`); `static/learn.js` (`?v=20260601a`) кнопка + `submitAnswer`; `static/style.css` (`?v=20260601a`) `.btn-reveal` + `.lfb-verdict.revealed`. Тест `test_srs_api.test_answer_reveal_forces_again`.

7. **SRS «правильно» для микс-карт = любое in-strategy действие.** На карточке `A5s UTG = {open: 0.7, fold: 0.3}` и open, и fold оцениваются как GOOD. Это знание про микс-природу спота, а не угадывание ГСЧ.

8. **Два режима тренажёра — Practice и Learn — это разные системы.** Learn использует SRS. Они не пересекаются, статистика отдельная.

    **Поправка от 2026-05-31 (по решению пользователя): Practice оценивает по членству в стратегии, а НЕ по броску ГСЧ.** Любое действие с частотой > 0 (включая implicit fold) считается верным — так же, как в Learn (#7). Для `66 = {open: 0.27}` и open, и fold верны; для `AA = {open: 1.0}` fold неверен (implicit fold нет, сумма = 1.0). Раньше «верность» определял бросок ГСЧ (`resolve_mixed_action`) → открытие микс-руки часто помечалось «неверно», хотя рука в диапазоне — это и был баг P-018. **ГСЧ-розыгрыш остаётся только как иллюстрация** («бросок сказал raise, в следующий раз может fold — не привязывайся»), он больше не решает правильно/неправильно. Реализация: `drill_engine.action_in_strategy / dominant_action / strategy_with_fold` + грейдинг в `check_answer`. Тесты: `test_drill_ev.py` (39).

9. **State per range file.** Каждый JSON-диапазон имеет свой `srs_state/<stem>.srs.json`. Cash и MTT не пересекаются в обучении.

10. **Code-style.** Английский в коде и комментариях. Английский в UI-строках в коде (UI-локализация на русский — не сейчас). Тесты — простые функции `test_*` запускаемые `python3 test_file.py`, без pytest.

11. **SRS-апгрейд отложен до накопления данных.** Текущий SM-2 остаётся как есть до тех пор, пока не закрыт frontend Learn Mode и не накопится 2-3 недели реальных ответов. После — Этап 1 апгрейда: добавить `difficulty: int` в `Card` (инициализируется по типу стратегии: pure → 1, near-pure → 3, ровный микс → 8–10) и начать логировать полную историю `(Δt_i, r_i)` в `srs_state/*.json`. Полноценная FSRS-математика (формулы DHP вместо graduation ladder) и тем более SSP-MMC с матрицей policy — отложены. На нашем масштабе (~1500 карточек, один пользователь) ROI от полного перехода низкий, а от Этапа 1 — высокий. Контекст и обоснование — в `docs/ssp_mmc_ru.pdf` (разбор статьи Ye et al. KDD'22, разделы 9 и 11). Не переходить к Этапу 2+ без явного решения «вот тут SM-2 систематически промахивается на моих данных».

    **Каркас FSRS-lite собран (2026-06-17), НЕ подключён к проду.** Рекомендация из `docs/fsrs_analysis.md` §5 реализована: `srs_fsrs.py` — планировщик поверх `py-fsrs` (FSRS-6) **параллельно** SM-2; `srs_api` его не импортит, живой Learn остаётся на SM-2. Конфиг `desired_retention=0.95` / `maximum_interval=120` / `learning_steps=()`; `seed_difficulty(strategy)` сидирует difficulty из частот (pure→2.0, near-pure→~4–5, ровный микс→8.0, мульти-вей→clamp 10; ключ — `max(freq)`, симметрично вокруг 0.5). A/B-харнесс `tools/fsrs_ab_compare.py` гоняет реальную историю из `srs_state/*.srs.json` через оба движка (на сегодняшних 15+10 ответах видно: SM-2 даёт `AA` и миксу `66` ИДЕНТИЧНУЮ лесенку, FSRS-lite — `66` ~2× чаще). `fsrs` — dev-only, НЕ в `requirements.txt` (импорт ленивый, прод не грузит); тесты `test_srs_fsrs.py` (45). **Под замком (по этому же #11):** фит параметров (`fsrs-optimizer`) до 2–3 недель данных, подключение FSRS-lite к `srs_api`, полный SSP-MMC. Детали — `docs/roadmap.md` A.4.

12. **Карты — реалистичная 4-цветная колода (SVG-ассеты), НЕ плоская плитка.** Рассматривали GTO-Wizard-стиль (вся карта залита цветом масти, ранг буквой) — отклонили: карты у нас «герой» (богатство), плитка — это дисциплина GTOW. Используем CC0-колоду `saulspatz/SVGCards` (vertical, jumbo-index), перекрашенную в 4 цвета: червы `#ae0d03`, бубны `#2235c5`, трефы `#33ae2b`, пики `#504f4f`. Ассеты в `cards/`, отдаются через mount `/cards` в `main.py`. Маппинг `rank+symbol → имя файла` — `cardSvgUrl()` в `drill.js`. **Вскрытые лица показываются только у hero (Drill/Learn) и на борде (постфлоп); оппоненты сидят рубашками. Лица оппонентов — только в анализаторе/парсере на шоудауне (Milestone 3).** Детали — `docs/design.md` (секция «Игральные карты») и `docs/roadmap.md` B.1-cards.

    **Поправка 2026-06-14 (по решению пользователя): реалистичная колода заменена на GTOW-плитку.** Стиль: плоская плитка цвета масти (4-цвет, углублённые под белый текст: червы `#b01217`, бубны `#1f3bb0`, трефы `#2a8a36`, пики `#3f3f42`), ОДНА огромная масть-вотермарка (opacity .26) с заходом за край карты (clip по форме), крупный белый ранг по центру, БЕЗ угловых индексов. Десятка рисуется буквой **«T»** (файл остаётся `<suit>10.svg`; `cardSvgUrl` в `static/cards.js` уже мапит `T → 10`). J/Q/K — тоже буквы-плитки (рисованных «дворов» больше нет). Масти — аккуратные **векторные** (острый кончик червы, тонкая пика, чёткий трилистник трефы, ромб-бубна), НЕ Firefly: гибрид с Firefly рассматривался, но для чистой плитки вектор чище/точнее, а Firefly-OAuth программно не стартует (нужен ручной `/mcp`) → отложен как опция. **Источник — `cards/gen_cards.py`** (`python3 cards/gen_cards.py` перерисует 52; `TILES_OUT=dir` пишет в staging). Прежняя колода (saulspatz, 4-цвет, с дворами) — в бэкапе **`cards/_backup_realistic_20260614/`** (откат: скопировать `*.svg` обратно в `cards/`). Интеграция drop-in: имена файлов те же → `cardSvgUrl` не менялся; cache-bust — версия на URL карты в `cards.js` (`?v=20260614a`) + бамп `cards.js?v=20260614a` в `index.html`. Рубашки (`goldBack.svg`) и постфлоп-борд (CSS) не тронуты. Превью — `cards_preview.html` + контактный лист.

    **Поправка 2026-06-14 (вторая, по решению пользователя): плоская плитка подогнана 1:1 под присланный референс → embossed-плитка. ПРИМЕНЕНО в боевую `cards/`.** Пользователь прислал картинку 4-цветной плитки (`docs/design_refs/gtow_cards_original_20260614.png`) и сказал «сделай как в референсе». Отличия новой колоды от прежней плоской: (1) цвета масти **сняты пиксельно с референса** (PIL) — heart tile `#982010`/emblem `#581008`, diamond `#2030b8`/`#102070`, club `#48a840`/`#286020`, spade `#504848`/`#302828` (+ едва заметный глянец-градиент ±12%); (2) масть-эмблема теперь **embossed** (тёмный кант снизу-справа + светлый сверху-слева) и **тёмного тона** (не белая полупрозрачная), КРУПНАЯ, центр ~0.70x/0.49y — смещена вправо с заходом за правый край; (3) ранг крупнее и по центру (fs 192 ≈ 44% высоты карты — промеры PIL по оригиналу); (4) угловой индекс так и НЕТ (промер подтвердил: в референсе его нет — первая реконструкция ошибочно его добавляла). Десятка «T», имена файлов и viewBox 210×315 прежние → `cardSvgUrl` не менялся. Источник тот же `cards/gen_cards.py` (переписан). Бэкап плоской плитки — **`cards/_backup_tile_20260614/`** (52 файла; откат копированием в `cards/`). Cache-bust поднят: URL карты в `cards.js` (`?v=20260614b`) + `cards.js?v=20260614b` в `index.html`. Проверено: 52/52 well-formed, контактный лист из живой `cards/` сверен с оригиналом (совпадает; единственная дельта — наша пропорция 2:3 vs ~0.60 у референса, требуется раскладкой стола). Staging `cards/_tiles_emboss/` осталась (unlink заблокирован средой, ср. P-019) — безвредна. Детали — `design.md` §10, roadmap B.1-cards. **Доводка (тот же день):** треф-эмблема собрана в ОДИН путь-объединение (3 перекрытых лоба + ножка, единая обмотка) вместо 3 отдельных `<circle>` — убирает шов по центру и alpha-stacking в полупрозрачных слоях эмбосса; cache-bust → `?v=20260614c`.

    **Рубашка оппонентов — собственный SVG-ассет, НЕ CSS-градиент (2026-06-03).** Раньше `.mini-back` (28×39, карты на сиденьях) и `.card-back` (64×92, заглушки борда + hero рубашкой вниз) рисовались синим CSS-градиентом + `::after`-оверлеем. Заменены на **один ассет `cards/goldBack.svg`** (`viewBox 0 0 64 92`): чёрный фон с тёплым радиальным центром, двойная золотая рамка (`--gold #c9a84c` / `--gold-hi #e8c97a`), симметричная решётка-гильош (clip по внутренней панели), угловые скобки и центральный медальон-ромб с золотым спейдом (бренд-эхо `static/favicon.svg`). Подключение — чистый CSS (`background: #060606 url(/cards/goldBack.svg) center/100% 100% no-repeat`), JS не трогали; `::after`-оверлеи убраны (рамку/текстуру несёт SVG). Cache-bust `style.css?v=20260603b`. Дизайн вёлся через Figma-плагин (файл «Poker Trainer — Card Backs», fileKey `ru5mUOYh3vzLeJwlkuQCIJ`), но из-за лимита Figma MCP на тарифе Starter финальный ассет собран генератором → SVG вручную (`cards/goldBack.gen.py`, `python3 cards/goldBack.gen.py` перерисует `goldBack.svg`; решётка считается с подрезкой по панели — симметрия гарантирована). `blueBack.svg` из колоды больше не используется.

    **Поправка 2026-06-22 (по решению пользователя): колода заменена на растровую 4-цветную из папки `LayIpoker`; рубашка → графит; добавлена D-фишка дилера.** Пользователь добавил в проект папку `LayIpoker` (графика покер-клиента) и попросил взять оттуда карты, значок дилера и рубашку (перекрасив в тёмный). (1) **Колода:** 52 лица `LayIpoker/Cards/<id>_1.png` (id 100 = рубашка; 101–113 трефы 2..A, 114–126 бубны, 127–139 червы, 140–152 пики — та же 4-цветная схема, что у нас: трефы зелёные / бубны синие / червы красные / пики серые) импортированы как `cards/<suit><rank>.png` (RGBA, drop-in под `cardSvgUrl`) генератором **`cards/gen_deck_layi.py`**. Это РАСТР (источник 134×186, hero рисуется 64×92 → запас 2×), пришёл на смену SVG-плитке. `cardSvgUrl` в `static/cards.js`: `.svg → .png`, cache-bust `?v=20260622b`. **Старая SVG-плитка осталась в `cards/*.svg` (другое расширение, не перезаписана) — путь отката = вернуть `.png→.svg` в `cardSvgUrl`.** (2) **Рубашка → графит:** `LayIpoker/cardgreen.png` (зелёная: решётка-ромб + медальон-спейд) перекрашена по яркости в графитовый ramp (генератор **`cards/recolor_back.py`**; оттенок «Графит» под палитру `#14161b`, ramp `#0c0d11→#1c1f26→#4a505c`, альфа/скругление сохранены) → **`cards/darkBack.png`** (120×163). `.mini-back` и `.card-back` в `style.css` переведены с `goldBack.svg` на `darkBack.png`. `goldBack.svg` больше НЕ используется (0 ссылок), но **оставлен для отката**. (3) **D-фишка дилера:** `LayIpoker/dealer.png` (бело-серебристая шайба с красной пикой) → **`cards/dealerButton.png`**; `.seat-circle.is-dealer::after` рисует её (`background-image`, `center/contain`) вместо золотого CSS-бейджа «D». **`button.png` (зелёная глянцевая пилюля) НЕ использован** — пользователь под «button» имел в виду именно D-фишку на столе, а не UI-кнопку (уточнено в чате). Cache-bust: `style.css?v=20260622a` + `cards.js?v=20260622b` в `index.html`; CSS-баланс скобок проверен (391/391), все 52 лица на месте, маппинг имя↔лицо сверен контактным листом. Бэкенд не затронут (чистый фронт/ассеты). Превью-композиты (сцена Drill + обзор колоды) — в `outputs`. **Открытый нюанс:** растровая рубашка графита тусклее прежней векторной `goldBack.svg` (это и есть «графит»); если на живом столе покажется слишком блёкло — поднять `HILITE` в `recolor_back.py` и перегенерировать.

    **Поправка 2026-06-23 (по решению пользователя): колода заменена на новую 4-цветную из папки `deck/`.** LayIpoker-карты «снова не понравились / негармоничны» → пользователь добавил папку **`deck/`** (по 13 PNG-скриншотов отдельных карт в подпапках `c/d/h/s`, имена — таймстемпы; каждая карта на зелёном фелте `~(35,101,37)` с мягкой drop-тенью) и попросил «обрезать и вставить». Стиль колоды: плоская плитка цвета масти, крупный белый ранг сверху-слева + символ масти, большой ранг снизу-справа, БЕЗ угловых индексов, десятка = «10». 4-цвет совпадает со схемой проекта (трефы зелёные / бубны синие / червы красные / пики тёмно-серые). **Обработка — собственный кроп+прозрачность, генератор `cards/gen_deck_from_screens.py`** (`python3 cards/gen_deck_from_screens.py`; вход `deck/`, выход `cards/<suit><rank>.png`): (1) фелт убран region-grow-заливкой от 4 углов (Euclid<12 к медиане-фелту — **трефовая зелёная карта на dist 14.5 НЕ затрагивается**, в этом вся хитрость: club-green близок к felt-green); (2) drop-тень вычтена явно (greenish И bright<0.78·felt) — заодно чинит подрезание глянцевого тёмного низа треф; (3) внешние 2px карты срезаны эрозией (остаток фелта/AA/тень концентрируются там), цвет карты «выцвечен» (bleed) наружу в AA-кольцо, альфа = анти-алиас rounded-rect (r≈9% ширины, ss=4); (4) нормализация на канвас **134×186** (как LayIpoker → drop-in под `cardSvgUrl`, имена прежние). **Все 52 ранга сверены подписанным контактным листом** (ряды масть × столбцы A K Q J 10…2) — критично, т.к. исходные имена-таймстемпы не несут ранга. Маппинг index→rank зашит в генераторе (`RANK` dict). Бэкап LayIpoker-колоды — **`cards/_backup_layi_20260623/`** (54 png; откат: копировать `*.png` обратно в `cards/`). Рубашка `darkBack.png` и `dealerButton.png` НЕ трогались. Cache-bust `cards.js?v=20260623a` + `cards.js?v=20260623a` в `index.html`. Источник `deck/` оставлен в проекте. Бэкенд не затронут (чистый фронт/ассеты). Превью — контактный лист + сцена на реальном `table.png` (в `outputs`). **Открытый нюанс:** пики тёмные `~(38,37,38)` — на очень тёмном UI (hero-рука под столом, `--surface-0`) контраст низкий; это свойство исходного арта (не перекрашивали без запроса) — если мешает, осветлить отдельным шагом.

    **Поправка 2026-06-23 (вторая — рубашка/баттон/блайнд-фишки из того же референса; по решению пользователя).** Из того же покер-клиента, что и колода (`deck/`), пользователь добавил папки `table/` (скрины стола, см. #14) и `chips/{sb,bb,bu}/` (фишки/баттон) и попросил «взять стол, карты оппонентов+иконки, фишки». (1) **Рубашка `cards/darkBack.png`:** в референсе рубашка оппонента — ПЛОСКАЯ бело-серая болванка (rounded-rect, лёгкий вертикальный градиент + тонкий кант, без узора). По решению пользователя перекрашена под тёмный UI → пересобрана как чистый **графит** (ad-hoc PIL: rounded-rect 120×168, ramp `#262a32→#16191f`, кант `#404650`, едва заметный внутренний контур; узора нет — верность референсу). Сменила прежнюю LayIpoker-графитовую (с решёткой/медальоном). Питает И `.mini-back` (сиденья), И `.card-back` (hero/заглушки). Бэкап `cards/darkBack_pre_ref_20260623.bak.png`. (2) **Баттон дилера `cards/dealerButton.png`:** жёлтая шайба «D» с оранжевым 3D-бортом, вырезана из `chips/bu/` (felt→прозрачность region-grow от центра). Сменила LayIpoker-баттон. Бэкап `cards/dealerButton_pre_ref_20260623.bak.png`. (3) **Блайнд-фишки:** `chips/sb/sb.png` (0.5 BB синяя) + `chips/bb/bb.png` (1 BB серебро) вырезаны в прозрачность → **`static/chip_art/coin_05.png`/`coin_1.png`** (НЕ перезаписывают Higgsfield clay-чипы `chip_*` в той же папке). Интеграция **чисто CSS, `chips.js` НЕ тронут** (низкий риск): `.coin.d-05`/`.coin.d-1` получают `background-image` (растр-лицо `cover` в эллипс-фейс), CSS-борт (box-shadow `--wall`) и `::after`-инлей номинала ОСТАЮТСЯ → стек/масштаб без изменений; `::before` спот-обод скрыт (растр несёт свой). **Только блайнды+баттон сейчас (решение пользователя «номиналы потом»):** 5/25/0.1 остаются на CSS casino-3D; в смешанной стопке (3-бет 8 = 5+1+1+1) низ — CSS-красный, верх — растр-серебро (чуть разная фактура, приемлемо до отдельного чата про фишки). Cache-bust `?v=20260623b`: `style.css`, url `darkBack.png`/`dealerButton.png`/`coin_*.png`. Сверки: CSS-скобки 466/466; PIL-симуляция `.coin` (одиночные блайнды + стопки 2.5/8) читается верно; PIL-композит сцены Drill когерентен на тёмном фоне. Бэкенд не затронут (0 `.py`). Превью — `static/cards_scene_preview.html` (реальные style.css+chips.js+ассеты, открывать через сервер). **Открытые нюансы:** рубашка теперь плоская (без узора) — верно референсу, нужен «богаче» → добавить медальон отдельно; растр-фишки низкого разрешения (исходник ~29px) — на крупном столе мягковаты.

13. **GTO-EV в Drill — реальные числа из отдельного блока `ev`, не синтетика.** EV хранится в опциональном top-level блоке `ev` (см. «Формат range-файлов»), НЕ внутри action-словаря. Показ (RFI, Этап 1): **EV только на правильном открытии**; правильный фолд → «Correct — fold» без EV; любая ошибка → «Wrong — correct action: X» без EV (храним только EV прибыльного действия, EV фолда всегда 0). Нет данных EV для руки/диапазона → пилюля EV прячется, тренажёр работает как раньше (чистое правильно/неправильно). **Поправка 2026-05-31: ВСЕ синтетические EV-константы удалены из `drill_engine` (`EV_CORRECT_OPEN`/`EV_WRONG_*`/`EV_TIMEOUT`).** EV теперь показывается ТОЛЬКО как реальное число из блока `ev` через `get_ev`, единообразно на всех спотах (RFI/vs_RFI/vs_3bet) и на таймауте: пилюля видна лишь на верном не-фолд действии при наличии данных, иначе скрыта. Генераторы vs_RFI/vs_3bet теперь тоже кладут `gto_ev` в payload (там данных пока нет → `None` → пилюля скрыта, без заглушки `+0.15`). Пилюля `#fbEv` самоподписана («EV +2.31 BB»). Тесты: `test_drill_ev.py` (44).

    **Editor сохраняет блок `ev` (2026-05-31).** `edApplyLoadedData` кладёт `data.ev` в `ed.ev` (deep-copy), `_edBuildRangeData` дописывает его обратно при сохранении (`if (ed.ev) out.ev = ed.ev`). Так пересохранение пака в редакторе больше НЕ стирает EV (раньше стирало — отсюда потеря данных NL25GTOW). EV редактор не редактирует, только переносит как есть; если правишь сами диапазоны, ev-записи могут устареть (лишние/недостающие — безвредны: get_ev отдаёт по руке, лишние игнорятся, недостающие → нет пилюли). Cache-bust `editor.js?v=20260531a`.

    **Тестовая сборка с реальными EV — `data/NL25GTOW.json` (заполнено 2026-05-30, ПОТЕРЯНО при пересохранении, ВОССТАНОВЛЕНО 2026-05-31).** Блок `ev` исчезал из файла (вероятно затёрт Editor'ом, который `ev` не пишет) — восстановлен 2026-05-31 по новым скриншотам, 362 записи, структурная проверка пройдена. **Открытый риск:** пересохранение пака в Editor снова сотрёт `ev`, пока `_edBuildRangeData` не научится его переносить (связано с P-008). Это первый и пока единственный range-пак с реальным GTO-EV. Числа сняты вручную со скриншотов матрицы GTOW NL25 (вкладка «стратегия + EV», число в правом нижнем углу клетки) и посверены по каждой позиции с пользователем. Блок `ev.RFI` заполнен для всех 5 RFI-позиций — **UTG, MP, CO, BTN, SB** (47/55/66/88/106 рук = числу играбельных рук). Именно на этом паке демонстрируется EV-пилюля в Drill: открой `NL25GTOW.json`, спот RFI, любая позиция — на правильном открытии показывается реальный посчитанный EV в BB. Проверка целостности: каждая рука с `open>0` или `call>0` в `spots.RFI` имеет ровно одну запись в `ev.RFI`, лишних/пропущенных нет; `range_engine.get_ev` отдаёт `float`, на фолд-руках `None`. SB-нюанс: в open/call-миксе EV обоих действий равны (индифферентность GTO) → одно число на руку; call-only руки SB (74s, 87o, 97o, J8o, Q7o, T4s) тоже имеют EV (их прибыльное действие — call). Остальные паки (`cash_micro_100bb`, `gto_100bb_mtt`) блока `ev` НЕ имеют → Drill там работает как раньше (чистое правильно/неправильно, пилюля скрыта). Детали и workflow съёма — `docs/roadmap.md` → «Drill — GTO-EV», Этап 2.

    **HTML-импортёр GTOW (2026-06-04).** `tools/gtow_parse.py` — вместо ручного съёма со скриншотов парсит сохранённый DOM страницы GTOW «Solutions» (вкладка «стратегия + EV») и пишет частоты+EV прямо в пак. Декодит стратегию из CSS-полосок ячейки (`background-image` цвета + `background-size` ширины: видимый сегмент цвета = разность соседних ширин), EV — из `.rtc_value`; читает только левую таблицу (`id="0_*"`, class `rtc_strategy_ev_range_normalized`), правую EV-таблицу (`id="1_*"`, цвета `var(--clr-…)`) игнорит. Спот/hero/villain определяет из ленты истории (`data-tst="hs_*_preflop_<POS>"`, активная карта = hero, число рейзеров → RFI/vs_RFI/vs_3bet). Маппинг цветов: синий=fold(implicit), зелёный=call, красные (Raise+Allin сливаются)→`open`/`3bet`/`4bet` по споту. Алиас позиций `HJ→MP` (GTOW зовёт 2-е место HJ). Пороги: действие с частотой <0.5% (округление до 0.00) отбрасывается, EV пишется только для рук с EV≠0. `--apply` делает таймстемп-`.bak` перед записью. Тесты `tools/test_gtow_parse.py` (6, на реальных ячейках CO vs_UTG — эталон совпал; парсер чуть точнее старого ручного съёма: ловит суб-1% слайсы, напр. call 0.02 у QJs, который пропустили глазами). Workflow и съём дампов — `tools/gtow_dumps/README.md`. **Не сделано:** реальные числа для большинства vs_RFI-пар и всех vs_3bet (нужны дампы соответствующих спотов — парсер готов, данных нет).

    **vs_3bet начат: UTG vs_MP залит парсером с EV (2026-06-05).** Первая пара в споте `vs_3bet` (раньше блок был пуст). UTG open 2.5 → MP(HJ) 3bet 8 → фолды по кругу → UTG отвечает. Импорт из `tools/gtow_dumps/utg_vs_mp.html` (генератор `_build_utg_vs_mp.py`): 38 рук (`4bet`+`call` микс, узкий ответ на 3-бет) + 20 EV. Действия в данных — только `4bet`/`call` (фолд implicit). **Нюанс:** на экране GTOW у спота 4 действия — Fold/Call/Raise 21.5(4bet)/Allin 100; **Allin сливается в `4bet`** (конвенция Raise+Allin → агрессивный бакет, как везде). Для Drill это значит, что отдельной кнопки Allin пока нет — заведена задача «Drill — кнопки vs_3bet (allin/4bet/call/fold)» в `roadmap.md`. **Ловушка съёма (как и с BB vs SB):** GTOW по умолчанию проматывает ход до следующего игрока (CO), а не оставляет на опенере — нужен дамп, где активная карта = опенер (UTG), заголовок матрицы = UTG. Детект парсера: 2 рейзера → vs_3bet, hero=активная карта, villain=последний рейзер.

    **vs_3bet UTG vs_CO залит (2026-06-05).** Вторая пара: UTG open 2.5 → CO 3bet 8 → UTG отвечает. Из `tools/gtow_dumps/utg_vs_co.html` (`_build_utg_vs_co.py`): 39 рук (`4bet`+`call`) + 20 EV. **vs_3bet UTG vs_BTN залит (2026-06-05):** UTG open 2.5 → BTN 3bet 8 → UTG отвечает; 38 рук + 20 EV (`utg_vs_btn.html`).

    **vs_3bet UTG vs_SB и vs_BB залиты (2026-06-05) — ряд UTG закрыт полностью.** UTG vs SB (39 рук / 11 EV, `utg_vs_sb3b.html`) и UTG vs BB (42 рук / 11 EV, `utg_vsbb3b.html`) — это полные DOM-дампы, сохранённые файлами (не реконструкция генератором), парсер прогнан напрямую. Теперь у UTG есть все 5 пар vs_3bet (MP/CO/BTN/SB/BB). **Ряды UTG и MP в vs_3bet закрыты полностью (2026-06-05):** MP vs CO (37/17), vs BTN (43/18), vs SB (41/12), vs BB (40/11) — все 4. **Ряд CO закрыт (2026-06-05):** CO vs BTN (41/19), vs SB (42/16), vs BB (41/13). **vs_3bet ЗАКРЫТ ПОЛНОСТЬЮ (2026-06-05) — все 15 пар.** BTN vs SB (42/21), BTN vs BB (43/21), SB vs BB (54/36, blind-vs-blind — самая широкая защита). Итог: UTG ×5, MP ×4, CO ×3, BTN ×2, SB ×1 = 15 hero×villain пар, у каждой частоты (`4bet`/`call`) + EV. Все три префлоп-спота `NL25GTOW.json` (RFI / vs_RFI / vs_3bet) теперь заполнены реальными GTO-данными из GTOW. Дальше по EV-данным — только постфлоп/др. паки (вне этого пака). **Метод съёма перешёл на «дамп-файлом»:** пользователь сохраняет страницу GTOW (DevTools → Copy outerHTML) в `tools/gtow_dumps/`, я парсю по имени — без вставки тяжёлого DOM в чат (экономит контекст; реконструкция-генератором больше не нужна).

    **BB vs_SB залит парсером с EV (2026-06-05) — `vs_RFI` закрыт полностью.** Спот `vs_RFI` BB vs SB (SB raise 3.5, BB защищается) импортирован из `tools/gtow_dumps/bb_vs_sb.html`. Пары не было — теперь 102 руки (очень широкая защита в closing-споте: call + 3bet) + 85 EV-записей. Это была последняя незаполненная пара: в `vs_RFI` теперь есть все 15 hero×villain пар. **Нюанс источника:** дамп прислан в чат текстом (не файлом) и приходил вместе с частью «SB лимп → BB» (ответ на лимп). Эта вторая часть в нашу схему RFI/vs_RFI/vs_3bet НЕ ложится (нет спота «vs limp/complete»; если прогнать парсером — 0 рейзеров → он положит её в `RFI.BB`, чего быть не должно), поэтому НЕ импортирована. `bb_vs_sb.html` собран генератором `tools/gtow_dumps/_build_bb_vs_sb.py` (реконструкция из вставленного DOM: 169 ячеек левой таблицы + история; парсер сам детектит vs_RFI BB vs SB).

    **BB vs_BTN залит парсером с EV (2026-06-05).** Спот `vs_RFI` BB vs BTN (BTN raise 2.5, SB фолд, BB защищается) импортирован из `tools/gtow_dumps/bb_vs_btn.html`. Пары не было — теперь 95 рук (очень широкая защита против стила: call + 3bet) + 84 EV-записи.

    **BB vs_MP залит парсером с EV (2026-06-05).** Спот `vs_RFI` BB vs MP (MP raise 2.5, остальные фолд, BB защищается) импортирован из `tools/gtow_dumps/bb_vs_mp.html`. Пары не было — теперь 78 рук (широкая защита: call + 3bet-микс) + 58 EV-записей.

    **CO vs_MP залит парсером с EV (2026-06-05).** Спот `vs_RFI` CO vs MP (UTG fold, MP raise 2.5, CO отвечает) импортирован из `tools/gtow_dumps/co_vs_mp.html`. Раньше пары не было вовсе — теперь 42 руки (`3bet`+`call` микс) + 19 EV-записей. Так закрыта дырка «CO vs_MP».

    **MP vs_UTG залит парсером с EV (2026-06-05).** Спот `vs_RFI` MP (HJ) vs UTG (UTG raise 2.5, MP отвечает) импортирован из дампа `tools/gtow_dumps/mp_vs_utg.html`. Заменил прежние 38 рук-без-EV: теперь 40 рук (`3bet`+`call` микс) + 14 EV-записей (остальные руки в диапазоне с EV=0 на экране → пилюли нет). Новый парс — полный супермножество старого (ничего не потеряно), плюс поймал суб-1% слайсы `JTs`, `T9s`. Так закрыта дырка «MP vs_UTG без EV».

    **SB RFI переимпортирован парсером (2026-06-05).** Спот «SB open 3.5bb vs BB» (все фолдят до SB) залит из DOM-дампа `tools/gtow_dumps/sb_rfi.html` через `gtow_parse.py --apply` — заменил прежний ручной съём SB. Теперь `spots.RFI.SB` = 108 рук (open+limp микс: GTOW даёт SB опцию complete/call, зелёные слайсы → `call`), `ev.RFI.SB` = 90 записей (18 рук в диапазоне с EV=0 на экране → пилюли нет, ожидаемо). Парсер поймал суб-1% call-слайсы, которые ручной съём пропускал (напр. `AJs call 0.02`, `KK/AKo/22 call 0.07–0.09`). Целостность проверена через `range_engine.get_ev`; тесты `tools/test_gtow_parse.py` (6) и `test_drill_ev.py` (44) зелёные.

    **squeeze — парсер + range_engine (Этап 2a, 2026-06-14).** `detect_spot` теперь считает И рейзеров, И коллеров перед hero (раньше только рейзеров — это и был P-026): `1 рейзер + ≥1 коллер = squeeze`, villain = пара `"<opener>-<caller>"`. Экзотика, которую схема пока не держит (лимп-пот `0×≥1`, сквиз-над-3бетом `2×≥1`, `>3` рейзеров), кидает `gtow_parse.UnsupportedNode` → CLI отказывается парсить (refuse), а не молча мислейблит — **refuse не обходится через `--spot`** (override остаётся только для отсутствующей истории). Маппинг тот же, что в #13: зелёный→`call`, красные (Raise+Allin)→`squeeze`, синий→implicit fold. Запись: `apply_to_pack` кладёт squeeze под **голый** ключ-пару (без `vs_`-префикса) в `spots.squeeze[hero]["<opener>-<caller>"]` + зеркально `ev.squeeze`. Чтение: `range_engine.get_squeeze_range(data, hero, "<opener>-<caller>")` и ветка `squeeze` в `get_ev` (villain_position = голая пара). Тесты: `tools/test_gtow_parse.py` (было 6 → стало 11: детект squeeze, маппинг+EV, 2 refuse-кейса, round-trip через `range_engine`); регрессий нет (7 реальных дампов детектятся как раньше, drill_ev 44 / srs 63 зелёные). **Не сделано (Этап 2b/3):** `drill_engine` (`SQUEEZE_SIZE`, генерация/грейдинг спота), `srs` (card_id `__squeeze__`, deck-билдер), фронт, `config.squeeze_options`. **Реальный узел ещё не залит** — ждёт дамп `tools/gtow_dumps/bb_sqz_btn_sb.html` (узел GTOW `F-F-F-R2.5-C`, hero=BB), метод съёма как для vs_3bet.

    **vs_squeeze — парсер + range_engine (Этап 2a, 2026-06-14).** Зеркало squeeze: герой ОТКРЫЛСЯ, его холд-кольнули ≥1, кто-то сквизнул (3-бет), ход вернулся к герою. `detect_spot`: `2 рейзера + ≥1 коллер` расщеплён по двум признакам — (1) **последнее действие = Raise** (сквиз только что прилетел → ответ на сквиз) против Call (колл 3-бета = сквиз-над-3бетом, остаётся refuse); (2) **hero == опенер** (`raisers[0]`). Оба → `vs_squeeze`. Иначе (герой — кол-коллер, не опенер) → `UnsupportedNode` refuse (v1 держит только опенера; опенер всегда ходит первым после сквиза). Действие: красные (Raise+Allin)→`4bet`, зелёный→`call`, синий→implicit fold (`RAISE_ACTION["vs_squeeze"]="4bet"`). **Ключ villain (решение пользователя): `"<opener>-<squeezer>-<caller(s)>"`** — опенер первым, сквизер вторым, кол-коллеры по посадке; голый (без `vs_`-префикса), как squeeze. Запись: `apply_to_pack` → `spots.vs_squeeze[hero][key]` + зеркально `ev.vs_squeeze`. Чтение: `range_engine.get_vs_squeeze_range(data, hero, key)` + ветка `vs_squeeze` в `get_ev` (villain_position = голый lineup). Алиас `HJ→MP` применяется и к коллерам в lineup (узел `R2.25-C-C-F-F-R13`, hero=UTG-опенер → ключ `UTG-BB-MP-CO`, не `...HJ...`). Тесты: `tools/test_gtow_parse.py` (было 11 → стало 15: детект, маппинг→4bet+EV, refuse «коллер vs сквиз», round-trip через `range_engine`); регрессий нет (drill_ev 44 / srs 63 зелёные). **Не сделано (Этап 2b/3):** `drill_engine` (генерация/грейдинг спота), `srs` (card_id `__vs_squeeze__`, deck-билдер), фронт, `config`. **Первый реальный узел залит:** `tools/gtow_dumps/nl10/test_dump.html` (UTG-опенер vs сквиз BB при кол-коллерах MP/CO) → `spots.vs_squeeze.UTG["UTG-BB-MP-CO"]` (19 рук + 41 EV) в **новом паке `data/GTOWNL10.json`** (Cash 6-max 100bb, первый GTOW-пак для NL10).

    **Инструментарий съёма дампов (2026-06-14).** Чтобы не копировать outerHTML каждого спота вручную: (1) **букмарклет** `tools/gtow_dumps/BOOKMARKLET.md` — кнопка-закладка сохраняет текущую страницу GTOW в `gtow_<HERO>_<ts>.html` (один клик/спот, без копипаста, без ToS-риска — это «save page», не скрейпинг); (2) **батч-импортёр** `tools/gtow_import_all.py <folder> [--file pack.json] [--apply]` — парсит всю папку, dry-run печатает таблицу spot/hero/villain/hands/ev, `--apply` сливает ВСЕ узлы в пак за один проход с ОДНИМ `.bak` (вместо `.bak` на узел), refuse-узлы (P-026) перечисляет и пропускает, предупреждает о дублях-перезаписях. Refactor `gtow_parse`: вынесены `pack_key()` и `merge_into_data(data, ...)` (чистый merge без IO), `apply_to_pack` теперь зовёт их. **Конвенция:** одна папка дампов на лимит (`tools/gtow_dumps/nl10/`, …), чтобы дампы разных стейков не смешивались в чужой пак. Браузером я НЕ управляю (нет инструмента в среде) — автоматизация это букмарклет+батч, а не «Claude кликает».

    **GTOWNL10: vs_RFI + vs_3bet залиты батчем (2026-06-16).** Папка `NL10_GTOW_2.5bbopen_100bb/` (30 букмарклет-дампов) прогнана через `tools/gtow_import_all.py … --apply` → `data/GTOWNL10.json`. Залито: RFI ×5 (перезаписаны свежими — SB 96→109, мелкие свопы у MP 59→57 / CO 69→67 / BTN 88; UTG 51 без изменений), **vs_RFI 10/15 пар**, **vs_3bet 13/15 пар** — всё с EV. Бэкап `GTOWNL10.json.20260616-150706.bak`. Старый `nl10/gtow_CO_…295739.html` (vs_squeeze, CO холодный коллер) сам отказан парсером (P-026) — не залит. Все `vs_3bet`-дампы проверены «hero == опенер» (холодных мислейблов нет). **Не хватало (на 16.06):** vs_RFI — `CO/BTN/SB/BB vs UTG` + `BB vs SB` (5 пар); vs_3bet — `UTG vs BB`, `SB vs BB` (2 пары). **Закрыто 17.06** (vs_RFI/vs_3bet 15/15 + vs_4bet 15/15 — см. roadmap), покрытие подтверждено `gtow_coverage.py` 18.06; остаются только iso (НЕ из GTOW) и squeeze/vs_squeeze-доборы. **Ловушка съёма (P-033):** дампы сначала пришли во вкладке «стратегия» (без EV) → класс ячеек `rtc_strategy_range_normalized` (без `_ev`), парсер видел 0 рук; пересохранены во вкладке «стратегия + EV» → `rtc_strategy_ev_range_normalized` → OK. `config.vs_rfi_options`/`vs_3bet_options` в паке всё ещё `{}` (как и `squeeze_options`) — фронт не покажет эти споты, пока их не заполнить. Тесты зелёные: gtow_parse 15, drill_ev 44, srs 64, ranges_api 7, srs_api 18.

    **GTOWNL50 (третий GTOW-пак) — все 4 префлоп-спота залиты (2026-06-17).** `data/GTOWNL50.json` (Cash 6-max 100bb, дерево «With cold calls 2.5x»): RFI 5/5, vs_RFI 15/15, vs_3bet 15/15, vs_4bet 15/15 = 50 узлов, частоты+EV; `ev` зеркалит `spots` 1:1, `config.vs_rfi_options`/`vs_3bet_options`/`vs_4bet_options` заполнены autoconfig'ом. Съём — браузером (Chrome MCP, URL-навигация), `gametype=Cash6m50zGeneral25Open3betV2SimpleAI` + отдельный `gmff_rake=NL50`; ВСЕ размеры дерева прочитаны ЗАНОВО с живого NL50-URL (рейк ≠ NL10 → сайзы другие: опен 2.5 / SB 3, 3-бет 7.5/10/9-11, 4-бет 20/22/23/22.5). Папка `tools/gtow_dumps/nl50/`, строка `nl50` в `limits.json`. Детали и полные сайзы — `docs/roadmap.md` (абзац GTOWNL50); уточнённый метод включения EV-вкладки (JS-диспатч mouse-событий по дропдауну режима; параметр режима — `soltab`, не `stratab`) — `problems.md` P-034.

    **GTOWNL100 (четвёртый GTOW-пак) — все 4 префлоп-спота залиты (2026-06-17).** `data/GTOWNL100.json` (Cash 6-max 100bb, дерево «With cold calls 2.5x»): RFI 5/5, vs_RFI 15/15, vs_3bet 15/15, vs_4bet 15/15 = 50 узлов, частоты+EV; `ev` зеркалит `spots` 1:1, `config.*_options` заполнены autoconfig'ом. Съём — браузером (Chrome MCP из Cowork, URL-навигация, тот же метод что GTOWNL50), `gametype=Cash6mGeneral_6mNL100R25` + `gmff_rake=NL100`; ВСЕ размеры прочитаны ЗАНОВО с живого NL100-URL (рейк ≠ NL50 → сайзы другие: опен 2.5 / SB 3.5, 3-бет 8/8.5/9/11.5-12/10.5-13, 4-бет 20-27.5). Папка `tools/gtow_dumps/nl100/`, строка `nl100` в `limits.json`. **Ключевое отличие NL100:** дефолтная вкладка GTOW открывается на 300bb — форсить `depth=100` в URL. Детали и полные сайзы — `docs/roadmap.md` (абзац GTOWNL100); две новые ловушки съёма (Chrome дописывает «(1)» к повтору имени; поллинг сохранения ждать реальные бары AA, не только 169 клеток) — `problems.md` P-034.

    **GTOWNL200 (пятый GTOW-пак) — все 4 префлоп-спота залиты (2026-06-17).** `data/GTOWNL200.json` (Cash 6-max 100bb, дерево «With cold calls 2.5x»): RFI 5/5, vs_RFI 15/15, vs_3bet 15/15, vs_4bet 15/15 = 50 узлов, частоты+EV; `ev` зеркалит `spots` (RFI break-even руки с EV=0 без пилюли — корректное подмножество), `config.*_options` заполнены autoconfig'ом. Съём — браузером (Chrome MCP из Cowork, URL-навигация, тот же метод что GTOWNL100), `gametype=Cash6mGeneral_6mNL200R25` + `gmff_rake=NL200`; ВСЕ размеры прочитаны ЗАНОВО с живого NL200-URL (рейк ≠ NL100 → сайзы другие: опен 2.5 / SB 3.5, 3-бет IP 8 / SB 11 / BB 13.5 (vs IP) · 10.5 (vs SB), 4-бет vs-IP 21.5 / vs-SB 23 / vs-BB 28.5 / SB-vs-BB 22). Папка `tools/gtow_dumps/nl200/`, строка `nl200` в `limits.json`. **На NL200 дефолт открылся уже на 100bb** (не 300 как NL100), но `depth=100` всё равно форсим. Тесты gtow_tools/gtow_parse/drill_ev/srs зелёные; ranges_api не прогнан (в песочнице нет fastapi, PyPI недоступен) — пак сверен загрузкой через `range_engine`. Детали и полные сайзы — `docs/roadmap.md` (абзац GTOWNL200); новая ловушка-8 («AA красная» не универсальный сигнал загрузки: в deep BvB vs_4bet AA — чистый call/зелёная; ждать «AA или KK не-фолд») + CDP-45s-таймаут на глубоких узлах — `problems.md` P-034.

    **GTOWNL500 (шестой GTOW-пак) — все 4 префлоп-спота залиты (2026-06-17).** `data/GTOWNL500.json` (Cash 6-max 100bb, дерево «With cold calls 2.5x»): RFI 5/5, vs_RFI 15/15, vs_3bet 15/15, vs_4bet 15/15 = 50 узлов, частоты+EV; `ev` зеркалит `spots` 1:1, `config.*_options` заполнены autoconfig'ом (по 15 пар). Съём — браузером (Chrome MCP из Cowork, URL-навигация, тот же метод что GTOWNL200), `gametype=Cash6mGeneral_6mNL500R25` + `gmff_rake=NL500`; ВСЕ размеры прочитаны ЗАНОВО с живого NL500-URL (рейк ≠ NL200 → сайзы другие: опен 2.5 / SB 3.5, 3-бет MP 8 / CO·BTN 8.5 / SB 11.5 / BB 13 (vs IP) · 10.5 (vs SB), 4-бет vs-MP-3беттор 21.5 / vs-CO·BTN 23 / vs-SB 24 / vs-BB 27.5 / SB-vs-BB 23). Папка `tools/gtow_dumps/nl500/`, строка `nl500` в `limits.json`. Дефолт открылся на 100bb (как NL200), `depth=100` всё равно форсим. **Сессия прошла чисто:** 0 блокировок множественных загрузок, 0 CDP-таймаутов, 0 NO_EV/NOT_LOADED на 50 узлах (EV-вкладка — JS-диспатч mouse-событий по каретке режима `.gw_pop_menu_opener` rect≈[85,179]→пункт «стратегия + EV»; гейт «загрузилось» = «AA или KK не-фолд», ловушка-8 учтена в поллере с самого начала → глубокий BB-vs-SB vs_4bet снялся без ложного NOT_LOADED). Тесты gtow_tools/gtow_parse/drill_ev/srs зелёные; ranges_api не прогнан (нет fastapi/PyPI в песочнице) — пак сверен загрузкой через `range_engine.load_range_file` + `get_ev` по 4 спотам. Детали и полные сайзы — `docs/roadmap.md` (абзац GTOWNL500); устоявшийся рецепт капче-функции — `problems.md` P-034 (NL500-уточнение).

    **GTOWNL1000 (седьмой GTOW-пак) — все 4 префлоп-спота залиты (2026-06-18).** `data/GTOWNL1000.json` (Cash 6-max 100bb, дерево «With cold calls 2.5x»): RFI 5/5, vs_RFI 15/15, vs_3bet 15/15, vs_4bet 15/15 = 50 узлов, частоты+EV; `ev` зеркалит `spots` 1:1 (0 сирот), `config.*_options` заполнены autoconfig'ом (по 15 пар). Съём — браузером (Chrome MCP из Cowork, URL-навигация, тот же метод что GTOWNL500), `gametype=Cash6mGeneral_6mNL1000R25` (сверен по Tab Context) + `gmff_rake=NL1000`; размеры прочитаны ЗАНОВО с живого NL1000-URL — **совпали с NL500 один-в-один** (рейк уже мал → сайзы сошлись; всё равно перечитываем, не предполагаем): опен 2.5 / SB 3.5, 3-бет MP 8 / CO·BTN 8.5 / SB 11.5 / BB 13 (vs IP) · 10.5 (vs SB), 4-бет vs-MP-3беттор 21.5 / vs-CO·BTN 23 / vs-SB 24 / vs-BB 27.5 / SB-vs-BB 23. Папка `tools/gtow_dumps/nl1000/`, строка `nl1000` в `limits.json`. Дефолт открылся на 100bb (как NL200/NL500), `depth=100` форсим. **Сессия прошла чисто (как NL500):** 0 блокировок загрузок, 0 CDP-таймаутов, 0 NO_EV/NOT_LOADED на 50 узлах; гейт «AA или KK не-фолд» снова снял глубокий BB-vs-SB vs_4bet без ложного NOT_LOADED (там AA — чистый call, EV 60.2; ловушка-8). Тесты gtow_tools 24 / gtow_parse 15 / drill_ev 44 / srs 64 зелёные; ranges_api не прогнан (нет fastapi/PyPI в песочнице) — пак сверен загрузкой через `range_engine.load_range_file` + `get_ev` по 4 спотам. Детали — `docs/roadmap.md` (абзац GTOWNL1000).

    **GTOWNL2000 (восьмой GTOW-пак) — все 4 префлоп-спота залиты (2026-06-18).** `data/GTOWNL2000.json` (Cash 6-max 100bb, дерево «With cold calls 2.5x»): RFI 5/5, vs_RFI 15/15, vs_3bet 15/15, vs_4bet 15/15 = 50 узлов, частоты+EV; `ev` зеркалит `spots` 1:1 (50 ключей, 0 сирот), `config.*_options` заполнены autoconfig'ом (по 15 пар). Съём — браузером (Chrome MCP из Cowork, URL-навигация, тот же метод что GTOWNL1000), `gametype=Cash6mGeneral_6mNL2000R25` (НЕ угадан — построенный URL загрузил реальный солв, header «6Max NL2k») + `gmff_rake=NL2000`; размеры прочитаны ЗАНОВО с живого NL2000-URL — **совпали с NL500/NL1000 один-в-один** (рейк уже мал → сайзы сошлись; всё равно перечитываем, не предполагаем): опен 2.5 / SB 3.5, 3-бет MP 8 / CO·BTN 8.5 / SB 11.5 / BB 13 (vs IP) · 10.5 (vs SB), 4-бет vs-MP-3беттор 21.5 / vs-CO·BTN 23 / vs-SB 24 / vs-BB 27.5 / SB-vs-BB 23. Папка `tools/gtow_dumps/nl2000/`, строка `nl2000` в `limits.json`. Дефолт открылся на 100bb (как NL200/NL500/NL1000), `depth=100` форсим. **Сессия прошла чисто (как NL500/NL1000):** 0 блокировок загрузок, 0 CDP-таймаутов, 0 NO_EV/NOT_LOADED на 50 узлах; гейт «AA или KK не-фолд» снова снял глубокий BB-vs-SB vs_4bet без ложного NOT_LOADED (там AA — чистый call, EV 60.2; ловушка-8). Тесты gtow_tools 24 / gtow_parse 15 / drill_ev 44 / srs 64 зелёные; ranges_api не прогнан (нет fastapi/PyPI в песочнице) — пак сверен загрузкой через `range_engine.load_range_file` + `get_ev` по 4 спотам. Детали — `docs/roadmap.md` (абзац GTOWNL2000).

    **GTOWNL5000 (девятый GTOW-пак) — все 4 префлоп-спота залиты (2026-06-18).** `data/GTOWNL5000.json` (Cash 6-max 100bb, дерево «With cold calls 2.5x»): RFI 5/5, vs_RFI 15/15, vs_3bet 15/15, vs_4bet 15/15 = 50 узлов, частоты+EV; `ev` зеркалит `spots` 1:1 (50 ключей, 0 сирот), `config.*_options` заполнены autoconfig'ом. Съём — браузером (Chrome MCP из Cowork, URL-навигация, тот же метод что GTOWNL2000), `gametype=Cash6mGeneral_6mNL5000R25` (сверен по Tab Context живого солва, header «6Max NL5k») + `gmff_rake=NL5000`; размеры прочитаны ЗАНОВО с живого NL5000-URL — **НЕ совпали 1-в-1 с NL500/1000/2000** (вопреки сходимости тех трёх — у NL5000 более тонкая градация, поэтому правило «читать заново» окупилось): опен 2.5 / SB 3.5, 3-бет MP 8 / CO·BTN 8.5 / SB 11.5 (но **12 vs MP**) / BB 12.5 (vs UTG/MP) · 13 (vs CO/BTN) · 10.5 (vs SB), 4-бет vs-MP-3беттор 21.5 / vs-CO·BTN 23 / vs-SB 24 / vs-BB 26.5 / MP-опенер vs SB 25 / CO·BTN-опенер vs BB 27.5 / SB-vs-BB 22.5. Папка `tools/gtow_dumps/nl5000/`, строка `nl5000` в `limits.json`. Дефолт открылся на 100bb, `depth=100` форсим. **Сессия почти чистая (50 узлов):** 0 блокировок загрузок, 0 CDP-таймаутов, 0 NO_EV/NOT_LOADED; единственная заминка — один транзиентный обрыв Chrome MCP в vs_4bet-батче (батч уже доснял+сохранил все 5 узлов до ошибки → потерь 0, восстановился на следующем вызове). Гейт «AA или KK не-фолд» снова снял глубокий BB-vs-SB vs_4bet без ложного NOT_LOADED (там AA — чистый call, EV 58.74; ловушка-8). Тесты gtow_tools 24 / gtow_parse 15 / drill_ev 44 / srs 64 зелёные; ranges_api не прогнан (нет fastapi/PyPI в песочнице) — пак сверен загрузкой через `range_engine.load_range_file` + `get_ev` по 4 спотам. Детали — `docs/roadmap.md` (абзац GTOWNL5000).

    **GTOWNL10000 (десятый GTOW-пак) — все 4 префлоп-спота залиты (2026-06-19).** `data/GTOWNL10000.json` (Cash 6-max 100bb, дерево «With cold calls 2.5x»): RFI 5/5, vs_RFI 15/15, vs_3bet 15/15, vs_4bet 15/15 = 50 узлов, частоты+EV; `ev` зеркалит `spots` 1:1 (50 ключей, 0 сирот), `config.*_options` заполнены autoconfig'ом. Съём — браузером (Chrome MCP из Cowork, URL-навигация, тот же метод что GTOWNL5000), `gametype=Cash6mGeneral_6mNL10000R25` (сверен по Tab Context, header «6Max NL10k») + `gmff_rake=NL10000`; размеры прочитаны ЗАНОВО с живого NL10000-URL — **совпали с NL5000 один-в-один** (тонкая градация NL5000 удержалась; всё равно перечитываем, не предполагаем): опен 2.5 / SB 3.5, 3-бет MP 8 / CO·BTN 8.5 / SB 11.5 (но 12 vs MP) / BB 12.5 (vs UTG/MP) · 13 (vs CO/BTN) · 10.5 (vs SB), 4-бет UTG vs MP 21.5 / vs CO·BTN 23 / vs SB 24 / vs BB 26.5 · MP vs SB 25 · CO·BTN-опенер vs BB 27.5 · SB-vs-BB 22.5. Папка `tools/gtow_dumps/nl10000/`, строка `nl10000` в `limits.json`. Дефолт открылся на 100bb, `depth=100` форсим. **Сессия прошла чисто (50 узлов):** 0 блокировок загрузок, 0 CDP-таймаутов, 0 NO_EV/NOT_LOADED; гейт «AA или KK не-фолд» снял глубокий BB-vs-SB vs_4bet без ложного NOT_LOADED (там AA — чистый call, EV 58.4; ловушка-8). Капчу гнал из Cowork батчами по 5 узлов (`browser_batch`: navigate+IIFE на узел; IIFE сам поллит загрузку, форсит EV-вкладку JS-диспатчем mouse-событий, читает hero-рейз = размер для следующего спота, сохраняет `outerHTML`→Blob в `~/Downloads`, возвращает только скаляры). Тесты gtow_tools 24 / gtow_parse 15 / drill_ev 44 / srs 64 зелёные; ranges_api не прогнан (нет fastapi/PyPI в песочнице) — пак сверен загрузкой через `range_engine.load_range_file` + `get_ev` по 4 спотам. Детали — `docs/roadmap.md` (абзац GTOWNL10000).

    **Полная автоматизация импорта — 4 инструмента (2026-06-16).** Граница автоматизации не сдвинулась (браузером я не управляю + скрейпинг GTOW = ToS-риск → клики по спотам остаются ручными через букмарклет, см. абзац выше). Доведено до автомата ВСЁ после кликов. (1) **`tools/gtow_coverage.py [pack]`** — чек-лист покрытия: ожидаемую матрицу RFI/vs_RFI/vs_3bet выводит из `config.positions`+`rfi_positions` по посадке (универсально, 6/8-max), сверяет с `spots`, печатает filled/missing + плоский список «осталось накликать». squeeze/vs_squeeze комбинаторны → present-only. (2) **`tools/gtow_autoconfig.py pack [--apply]`** — чинит P-033-следствие: перестраивает `vs_rfi_options`/`vs_3bet_options`/`vs_4bet_options`/`iso_options`/`squeeze_options`/`vs_squeeze_options` из фактических `spots` (голые villain-позиции, сортировка по посадке; только пары с непустыми данными → фронт не предложит пустых матриц). Идемпотентно (данные — источник истины). **`squeeze_options`/`vs_squeeze_options` фронт пока НЕ читает** — пишутся на будущее. **`GTOWNL10.json` уже прогнан — config заполнен, vs_RFI/vs_3bet теперь видны на фронте.** (3) **`tools/gtow_import_limits.py [--apply] [--limit X]`** — оркестратор: мэппинг `tools/gtow_dumps/limits.json` (лимит→{folder,pack}), на каждый лимит за один проход: импорт спотов → обновление config (autoconfig) → отчёт покрытия, ОДИН `.bak` на пак. (4) **`tools/gtow_sort_downloads.py [--apply] [--limit X]`** — раскладывает `~/Downloads/gtow_<limit>_*.html` по `tools/gtow_dumps/<limit>/` (лимит из имени файла). **Букмарклет обновлён:** спрашивает лимит-тег (prompt с памятью в localStorage) и пишет самоописательное имя `gtow_<limit>_<hero>_<action-sig>_<id>.html` (имя — только подсказка, импортёр пере-детектит спот сам). **Гард от устаревших дампов:** `gtow_import_all.warn_overwrites` печатает `⚠ SHRINK`, когда входящий дамп ужимает уже залитый спот (старый/худший ре-капчур, напр. SB RFI 109→96) — папка должна держать АКТУАЛЬНЫЕ дампы. Рефактор `gtow_import_all`: `process_folder`/`print_parsed`/`apply_parsed`/`warn_overwrites` (импортируемы). Тесты `tools/test_gtow_tools.py` (24: expected-matrix, coverage-diff, derive-options+идемпотентность, shrink-guard, limit-token). Поток одной сессии: накликал букмарклетом → `gtow_sort_downloads.py --apply` → `gtow_import_limits.py --apply`. Дыры GTOWNL10 (на 16.06): vs_RFI 5 пар (`CO/BTN/SB/BB vs UTG`, `BB vs SB`), vs_3bet 2 (`UTG vs BB`, `SB vs BB`) — **закрыты 17.06; coverage всех 6 GTOW-паков чистый, подтверждено 18.06**.

    **Автозахват спотов (Playwright) — каркас написан, навигацию дожимает десктоп-Claude (2026-06-16).** Пользователь согласился на ToS-риск (автообход GTOW > «save page»; риск бана платного аккаунта — его решение). Я (Cowork) браузер не запускаю → написал каркас, живую привязку к GTOW делает десктопное приложение Claude (у него браузер есть). (1) **`tools/gtow_capture_list.py pack [--limit] [--out nodes.json]`** — из `gtow_coverage.missing` генерит машинный список узлов: spot/hero/villain + **рецепт действий по местам** (`[["UTG","open"],["MP","fold"],…]`, выведен из посадки) + человекочитаемое описание + out-путь. (2) **`tools/gtow_autocapture.py nodes.json [--mode guided|auto] [--dry-run]`** — Playwright (lazy-import, `--dry-run` без браузера), persistent-context для логина GTOW. **`guided`** (работает сразу, браузерных селекторов не требует): ты переходишь к узлу, скрипт сам сохраняет DOM + **сразу парсит через `gtow_parse` и печатает OK/⚠ REDO с числом рук** — мгновенно ловит P-033-вкладку и кривую навигацию + сам именует/кладёт в папку лимита. **`auto`** (полный хэндс-офф) — через хук **`navigate_to_node`**, который НАМЕРЕННО `NotImplementedError` пока не вписан: десктоп-Claude должен сперва проверить вживую, **кодируется ли узел GTOW в URL** (тогда `page.goto(url)` — надёжно) или только кликами по ленте (хрупко). Хуки `navigate_to_node`/`ensure_ev_tab` — единственные site-coupled места, помечены громко; селекторы EV-вкладки — догадка, проверить на живом DOM. **Скрипт НЕ прогонялся (нет браузера в Cowork) — отлаживается на Mac;** `verify_dump`/`--dry-run`/capture_list проверены (парсят реальные дампы). Профиль браузера `tools/gtow_dumps/.browser_profile/` (куки/сессия GTOW) — в `.gitignore`, НЕ коммитить. Установка на Mac: `pip install playwright && playwright install chromium`. Поток: `gtow_capture_list.py … --out nodes.json` → `gtow_autocapture.py nodes.json --mode guided` → `gtow_import_limits.py --limit … --apply`.

14. **Стол — отрендеренный растровый ассет, НЕ CSS-градиенты (2026-06-03).** «Герой» (стол: фелт+борт) больше не рисуется чистым CSS — переходим на отрендеренную картинку-подложку + HTML-слой поверх (архитектура как у GG). Пересматривает прежнее «без бинарных ассетов». Файлы: `static/table_art/render_table.py` (детерминированный рендерер — объёмный валик+монолитный фелт без шума), `static/table_art/table.png` (1500×860 RGBA, одобрен). Интеграция: `<img class="table-bg">` в `.table-wrap` + `<div class="table-area">` (абсолютно позиционированный оверлей на фелт-прямоугольник). Геометрия: left 4.667% / top 9.07% / width 90.67% / height 79.07% от размеров картинки (стадион 2:1 сохранён). Наклон (perspective tilt) — не решён, пока стол плоский. **Среда:** macOS TCC блокировала доступ к ~/Desktop в сессии 2026-06-03 — Python-рендер запускался в /tmp, PNG копировался `cp`. При повторении: Системные настройки → Конфиденциальность → Файлы и папки/Полный доступ → выдать Claude Code и Terminal доступ к Desktop. Детали — `docs/problems.md` P-019.

    **Поправка 2026-06-17 (Higgsfield MCP — пайплайн арта; по решению пользователя).** Растровые ассеты-героя теперь генерим через **Higgsfield MCP** (кастомный коннектор Claude, URL `https://mcp.higgsfield.ai/mcp`, вход аккаунтом — НЕ API-ключ; работает в Cowork). Стол — модель **Nano Banana Pro** (фотореализм, до 4k, 16:9), ~2 кредита/картинка на 2k; поток `generate_image` (async → `status:pending`) → поллинг `show_generations`/`job_display`. **Ограничение среды:** песочница НЕ тянет CDN Higgsfield (`cloudfront.net` → 403 у прокси; PyPI тоже закрыт, scipy не поставить) → готовую картинку пользователь скачивает и кладёт в папку проекта, дальше обработка локально (PIL/numpy). Стол заменён: Firefly → **Higgsfield Nano Banana Pro, фелт sage/desaturated-olive** (`table.png` 1500×860 RGBA; surround вырезан numpy border-flood'ом T=30; вписан в **прежний footprint** старого ассета → `.table-area` и посадка мест НЕ менялись; бэкап `static/table_art/table_pre_sage_20260617.bak.png`, исходник 2k `table_sage_src_20260617.png`; cache-bust `?v=20260617a` на 3 `<img class=table-bg>`). **Новое направление (по решению пользователя):** на очень тёмном UI (`--surface-0 #080d0a`) стол читается блёкло → весь визуал интерфейса будем переделывать через Higgsfield. План — `docs/roadmap.md` Track B.6 и `docs/design.md` §8.

    **Поправка 2026-06-19 (фелт перекрашен в яркий казино-зелёный; по решению пользователя).** Sage-фелт читался блёкло/серо на тёмном UI → запрос «ярче и контрастнее, как реальный казино-зелёный, реализм оставить». **Стол НЕ перегенерировали** (это меняет геометрию → перекалибровка мест) — перекрасили ТОЛЬКО фелт существующего ассета **локально** (PIL/numpy, без Higgsfield/CDN): борт/свет/текстура/форма-стадион/прозрачный surround и **footprint остались байт-в-байт** (`.table-area` и посадка мест НЕ трогались; правился только cache-bust). Маска фелта = опак-пиксели с «зеленоватостью» `G−max(R,B)≥4` (серый борт и прозрачный surround исключены), край feather 2px box-blur; яркость фелта (= запечённый световой градиент центр→край) с усилением контраста ×1.18 накладывается на 3-стоп зелёный ramp shadow→base→hi. **Воспроизводимый генератор `static/table_art/recolor_felt.py`** (`python3 recolor_felt.py`; вход — sage-бэкап, выход — `table.png`; `VARIANT` переключает `A_classic`/`B_vivid`/`C_emerald`). Пользователю показаны 3 варианта контрольным листом на фоне `--surface-0`; выбран **B_vivid** (центр ≈`#3fb267`, ramp `#0c4a28`→`#198a4a`→`#3fb267`). Бэкап sage — `table_pre_casino_green_20260619.bak.png` (откат: копировать в `table.png`); cache-bust `?v=20260619a` на 3 `<img class=table-bg>`. Это снимает «блёклый стол» из триггера B.6 — остаток B.6 теперь про окружающий UI, не про стол. Детали — `docs/design.md` §8, `docs/roadmap.md` B.1-table-art.

    **Поправка 2026-06-23 (стол заменён на ассет из покер-клиента-референса; по решению пользователя).** Пользователь добавил папку `table/` (скрины стола того же клиента, что колода `deck/` и фишки `chips/`) и попросил «обрезать вокруг стола как нужно». Higgsfield-стол сменён на **плоский клиентский стол** (зелёный фелт + тёмный мягкий борт-валик), вырезанный из `table/Знімок екрана …154303.png` (1250×749). **Surround там — тёмный текстурный олив** (НЕ ровно-чёрный; яркость пересекается с бортом → порог-детект не отделяет) → альфа **синтезируется**: felt-маска (зелёный) дилейтится на ~20px (`MaxFilter 41`) = felt+борт, остальное прозрачно (чистый стадион, immune к шуму фона). Вписан в **прежний footprint** (felt источника масштабируется в old-felt-rect `x[133,1364] y[130,720]` на канвасе 1500×860, X/Y независимо ~1.17/1.25) → `.table-area` и посадка мест НЕ тронуты, drop-in (результат-felt сошёлся с таргетом в пределах 1px). Боевой `table.png` (1500×860 RGBA). Бэкап Higgsfield-стола — `static/table_art/table_pre_layiref_20260623.bak.png` (откат: копировать в `table.png`). Cache-bust `?v=20260623b` на 3 `<img class=table-bg>`. Фелт оставлен в цвете референса (`~#236a23`, классический казино-зелёный) — НЕ перекрашивали (верность референсу; на тёмном UI читается). `recolor_felt.py`/Higgsfield-пайплайн больше не на пути (стол теперь клиентский растр), бэкап+генератор оставлены для отката. Рубашка/баттон/фишки из того же референса — #12 поправка 2026-06-23 (вторая). Детали — `docs/design.md` §8.

15. **Глубины стека и размеры открытий — отложены, путь добавления зафиксирован (2026-06-14).** В GTOW выбираются и глубина (100/200/300bb), и размер открытия (2/2.25/2.5/3bb/GTO-сайз). Сейчас НЕ реализуем ни то, ни другое — у одного игрока на микрах ценность точного сайза и нестандартных глубин низкая, а цена реализации высокая. Принцип: не платим сложностью за функцию, которой ещё не пользуемся, но оставляем чистый аддитивный путь. **Две независимые оси, решаются по-разному:**

    **Ось 1 — глубина стека.** Уже заложена на уровне `meta.stack_depth`: глубина = свойство всего пака, один файл = одна глубина (`cash_100bb_6max.json`, `cash_200bb_6max.json`, …). Добавление новой глубины — это новый range-файл, **ноль правок движков** (SRS-стейт и статистика у каждого пака свои по `<stem>`). Пока остаёмся на 100bb; заводим новый пак в день, когда реально тренируем другую глубину. Заранее не закладываемся.

    **Ось 2 — размер открытия.** Различать два случая. **(A) Разные солвы под фиксированный сайз** (GTOW даёт цельную стратегию под «2.5bb», другую под «3bb») — решается как ось 1: сайз уходит в `meta`/имя пака, без правок движков. **(B) Микс размеров ВНУТРИ одного солва** (одна рука миксует open 2.5bb 60% + open 5bb 25% + fold 15%) — наш формат это сейчас ТЕРЯЕТ: парсер по конвенции #13 сливает все красные сегменты (Raise+Allin) в один бакет `open`, размер не пишется (имена действий плоские: `open`/`3bet`/`4bet`).

    **Когда понадобится случай B — реализуем по аналогии с блоком `ev` (#13), это решённый дешёвый паттерн.** Добавляем отдельный top-level блок **`sizing`**, параллельный `spots` (НЕ внутрь action-словаря — иначе подхватят `available_actions`/грейдинг, как и с `ev`), зеркалящий структуру `spots`; значения — распределение размеров per-hand: `sizing.RFI.UTG.AKs = {"2.5": 0.6, "5.0": 0.25}` (fold вне sizing). Лукап `range_engine.get_sizing(...)` → `dict | None`; `None` → размер не показываем. Грейдинг остаётся **бинарным** («рука в диапазоне открытия → верно»), drill/SRS блок игнорят — `sizing` только для показа в UI («GTO открывает 2.5bb, иногда 5bb»). Парсер дописать реально: размеры в DOM GTOW есть (кнопки сайзов + оттенки красного на сегментах ячейки), сейчас намеренно схлопываются — надо перестать схлопывать и писать в `sizing`. **Полный тренажёр выбора размера** (сайз = действие, drill/SRS заставляют выбрать правильный размер, кнопки/card_id/грейдинг по размеру) — это уже подход поверх `sizing`, трогает весь стек (формат, range_engine, drill_engine, srs, фронт, парсер); делаем только когда диапазоны открытия уже уверенно выучены и осталось докрутить «чем». Не начинать без явного решения. Подробности — `docs/roadmap.md` → отложенная задача «Размеры открытий / глубины стека».

16. **Стек многопользовательской версии (Track D) — Supabase (managed Postgres + Auth). Решено 2026-06-17, ещё НЕ реализовано.** Хранилище (D.1) и авторизация (D.2) — один сервис **Supabase**: managed Postgres (= БД, в которую мигрируем JSON `srs_state`/`stats`/`history` с колонкой `user_id`) + Supabase Auth (email/пароль + вход через Google OAuth из коробки). **Почему Supabase, а не Firebase/GCP:** проект реляционный (SQL/Postgres) — Supabase это и есть Postgres, схлопывает D.1+D.2 в один сервис; Firebase даёт NoSQL (Firestore — плохо ложится на наши связанные данные) либо два сервиса (Identity Platform + платный Cloud SQL). Плюс Supabase — открытый Postgres (не запирает: данные можно выгрузить и уехать на Railway-Postgres) и учит переносимому SQL. **Google OAuth — это способ входа ВНУТРИ Supabase, НЕ альтернатива ему:** OAuth лишь подтверждает личность, а юзеров/сессии/email-поток держит Supabase. **Отклонено:** Clerk + отдельный Railway-Postgres (auth-only, два сервиса). Приложение остаётся на Railway (FastAPI), подключается к Supabase по сети. Бесплатного тарифа (50k MAU + Postgres) хватает на бету; free-проект засыпает после недели простоя → для прод-беты позже Pro ~$25/мес. Этапы и платежи — `docs/roadmap.md` Track D (обновление 2026-06-17). **Статистика в multi-user версии = единый append-only журнал событий `answers`** (каждый ответ Drill/Learn — строка); метрики по дням/спотам, дневные цели (`goals`) и баннер-напоминание — запросы над ним, а `stats.json`/`summarize()` становятся производными. Детали и решения — там же, «Статблок — подготовка к вебу».

17. **Глобальный chrome-редизайн + палитра матрицы — ПРИМЕНЕНО в живой код 2026-06-18 (из прототипа `Preflop Visualizer.dc.html`, хэндофф в `handoff/`).** Прокатан утверждённый на прототипе визуальный заход, четыре связанных изменения: **(1) Нейтрали → холодный графит.** Обвязка ушла с charcoal-green подтона на нейтральный графит, чтобы зелёная матрица «выстреливала», а не сливалась. Токены `:root`: `--bg #080d0a→#0a0b0e`, `--panel #0d1510→#14161b`, текст `--text/-mid/-dim` → `#e6e8ec/#aab0bc/#79808c`; добавлены `--surface-2 #1c1f26` / `--surface-3 #21242b` / `--recessed #0e0f13` / `--stroke #2f333b`. Зелёные хардкоды обвязки (тумблеры, слайдеры, скроллбар, поповеры-подсказки, аватар позиции, пустая клетка матрицы, well hint-грида) переведены на эти токены. **Фелт/золото/`--c-*` экшен-кнопки/`.d-*` фишки/correct-wrong — НЕ тронуты** (это «герой»/семантика, не обвязка). **(2) Ребренд `NLH TRAINER` → `PreflopRange`** (домен prefloprange): вордмарк = золотой скруглённый «P» + «Preflop» + «Range» золотом (`.logo-mark`/`.logo-text`/`.logo-accent`), `<title>PreflopRange`. **(3) Шрифт Oxanium → Hanken Grotesk** (по решению пользователя — и UI, и body; Oxanium давал «game-HUD»-вид). `--font-ui`/`--font-body` + Google Fonts-линк. **(4) Палитра матрицы (§9.1) РЕШЕНА:** вынесена в `static/mtx_palette.js` (`window.MTX = {COLORS, splitGradient, rfiFill}`, грузится первым в `index.html`); хардкоды в `visualizer.js`/`editor.js`/`drill.js` заменены на `MTX.*`, мёртвые `rfiColor`/`_hintRfiColor`/`_hintGradient` удалены. Единая split-бар модель: **RFI больше не сплошной бакет** — это open-цвет шириной = частоте опена (`MTX.rfiFill`). Цвета приглушены под графит (open `#2f9152`, call `#3c78a8`, 3bet `#c8433a`, 4bet `#8e2f2a`, allin `#6e2622`, fold `#121a15`, div `#0b100c`); лейбл `#eef0f3`+тень, пустая клетка `#52586399`; зеркальные CSS-токены `--mtx-*`. **Проверка:** node-тест доказал геометрию split-бара байт-в-байт = старый код; `node --check` ×4; бэкенд-тесты (drill_ev 44 / srs 64 / fsrs 32) не задеты (фронт-only). Cache-bust `?v=20260618a` на `style.css`/`mtx_palette.js`/`drill.js`/`visualizer.js`/`editor.js`. Превью — `static/redesign_preview.html` (грузит реальные ассеты). Это база под Track B.6 (богатство/фактуры через Higgsfield — отдельно). Детали — `docs/design.md` §2.1/§2.2/§9.1, `docs/roadmap.md` Track B.

    **Легенда Визуализатора — под матрицей, не в боковой панели (follow-up, 2026-06-18).** Правая панель-легенда (`aside.viz-legend-panel` со статичными старыми бакет-цветами, уже не совпадавшими с `MTX`-клетками) убрана; легенда теперь горизонтальная полоса `.viz-legend#vizLegend` прямо под `.matrix-wrap`, грид Визуализатора `.layout.viz-layout` → `190px 1fr` (2 колонки). Строится динамически по споту из `MTX.COLORS` (`buildVizLegend(spot)` в `visualizer.js`) — свотчи всегда = заливке клеток. Меньше беготни глазами (дух P-016). **Фикс разрыва (тот же день):** матрица+легенда обёрнуты в один центрированный `.matrix-stack` — иначе `.matrix-wrap{flex:1}` растягивался и в споте vs (левая панель выше → грид-строка растёт) между сеткой и прижатой к низу легендой зияло пустое поле + прокрутка; в стеке легенда всегда вплотную (gap 14px), группа центрируется. Проверка: node-стаб прогнал `buildVizLegend` по 5 спотам (лейблы + MTX-цвета + нет старых hex), `node --check`, баланс CSS 380/380. Cache-bust `style.css` → `?v=20260618c` (visualizer.js `?v=20260618b`). Детали — `docs/design.md` §9.1.

18. **Схема БД для веба — единый журнал событий `answers` + `cards`(SRS)/`goals`/`profiles`. Спроектирована 2026-06-19, ещё НЕ реализована (Track D, Этап 0).** Под план тренировки + дашборд (неделя/месяц, стрики) + честное определение тренировки (запрос пользователя 2026-06-19; выбран путь «сразу Postgres/Supabase», не файловый стопгап). Решения: **(1)** центр — **append-only `answers`** (строка на каждый ответ Drill+Learn, полная `ts`, `rating` для будущего FSRS-фита); все метрики (stats/history/по дням/стрики/прогресс целей) = запросы над ним, `stats.json`/`history.json`/`summarize()` становятся производными вьюхами. **(2)** `cards` = текущее SRS-состояние per-user; `card.history[]` из JSON **НЕ дублируется** — переезжает в `answers` (история карты = выборка по `card_id`); добавлены **nullable `stability`/`difficulty`** под FSRS A.4 (выкатка без миграции). **(3)** «День» = **локальный день пользователя**: `ts` в UTC, бакетинг `at time zone profiles.timezone` (иначе стрик рвётся на ночных сессиях). **(4)** Анти-фарм: `profiles.session_min_hands` (дефолт **20**, настр.) — день засчитан как тренировка только при ≥N рук; календарь/стрики строятся лишь по таким дням (вьюха `v_session_days`). **(5)** `goals` гибкие: `mode` + охват (`pack/spot/position/villain`, NULL=любой) + `period` (daily/total) + `metric` (hands/correct/new_cards); пример «100 опенов на позицию» = строки `goals`. **Learn-цель = `new_cards`** (повторы планирует SRS, форсить нельзя — mode-aware/FSRS-aware). Артефакты: `db/schema.sql` (DDL, авторитетно), `db/queries.sql` (запросы фич), `tools/migrate_to_pg.py` (экстрактор текущих файлов), `tools/verify_schema_sqlite.py` (верификация логики на sqlite — в песочнице нет Postgres/PyPI; **все проверки зелёные** на реальных 1859 cards/225 answers + многодневной синтетике: анти-фарм, оба стрика, календарь, цели daily/total, new_cards). Полный разбор + поэтапный план по чатам — `docs/db_schema.md`. Реализация (Supabase → API → auth → дашборд) — следующие чаты; **секреты только в env vars**.

    **Этап 1 начат — Supabase поднят, схема применена к ЖИВОЙ БД (2026-06-22).** Проект Supabase `Prefloprange.com` (организация `prefloprange.com`, project ref `ewarfrzaxiiqboaixbwq`, регион AWS `eu-central-1`/Frankfurt, план Free, compute NANO) — живой, статус Healthy. `db/schema.sql` применён через SQL Editor **без правок**: вставлен в Monaco-редактор как gzip+base64 → `DecompressionStream` (чтобы backticks/em-dash в комментариях не сломали передачу), сверен SHA-256 = `79585936…fcdc41a1` (11179 B) — байт-в-байт с файлом. Read-back через `information_schema` подтвердил все 8 объектов: таблицы `answers`(17 кол.)/`cards`(17)/`goals`(12)/`profiles`(5) + вьюхи `v_daily_activity`(6)/`v_session_days`(3)/`v_stats`(6)/`v_streaks`(3). **Также применён `db/supabase_addendum.sql`** (вторая половина D.1-supabase, тем же gzip+base64-методом + read-back): **FK `profiles.id → auth.users(id)`** (`on delete cascade`, подтверждён в `pg_constraint`: `profiles_id_fkey`→`auth.users`) и **RLS включён** на всех 4 таблицах (`relrowsecurity=true`×4; без политик → deny-by-default для anon/auth-ролей, а сервер ходит через direct-`postgres` → bypass RLS, не ломается). **НЕ сделано (осознанно, D.2):** RLS-политики `auth.uid()=user_id` и триггер `handle_new_user` (оставлены закомментированными в addendum).

    **D.1-api — слой подключения написан (2026-06-22, ждёт живой проверки).** `db.py` — ленивый `psycopg_pool.ConnectionPool` из `DATABASE_URL` с **мягкой деградацией** (нет переменной → пул не строится, `ping()` отдаёт `configured:false`, приложение работает на JSON как раньше → текущий прод на Railway без `DATABASE_URL` НЕ ломается; psycopg импортится лениво внутри `get_pool`, поэтому `import db` не падает без драйвера). `db_api.py` — роутер `GET /api/db/health` (`SELECT 1`+`now()`, ошибки ловит, пароль не светит). `main.py` += 1 строка `include_router(db_router)`. `requirements.txt` += `psycopg[binary]`/`psycopg_pool`/`python-dotenv`; `.env.example` (шаблон) + `.env`/`.env.local` в `.gitignore`. Драйвер — **psycopg 3** (чистый SQL под `db/queries.sql`; placeholders `:name`→`%(name)s` конвертируем на шаге записи), по решению пользователя. Проверено в песочнице: `py_compile` db/db_api/main, деградация без `DATABASE_URL`, `test_srs` 64 + `test_drill_ev` 44 зелёные. **Живой коннект к Supabase из песочницы НЕ проверяется** (нет сети + секрет нельзя тащить) → подтверждает пользователь через `/api/db/health` с Session-pooler `DATABASE_URL` в env. Следующий шаг — запись в `answers` (Drill+Learn), SRS-состояние → `cards`. **Безопасность:** connection string / anon+service ключи / db-пароль — секреты, в код/чат/доки НЕ писать, только env vars (Railway secret + локальный `.env` в `.gitignore`); project ref секретом не является (виден в URL дашборда). **Дальше:** API-слой (FastAPI пишет в `answers`, SRS-состояние → `cards`), затем D.2 (auth + FK + RLS), дашборд-фронт.

    **D.1-api-write — запись ответов в журнал `answers` (2026-06-22, dual-write).** Оба тренажёра логируют каждый ответ в `answers`. Новый модуль **`journal.py`** — единый write-path (соглашение «один модуль — одна задача»): (1) чистые маппинги `drill_answer_row(drill_hand, result)` / `learn_answer_row(card, …)` — трейнер-payload → строка журнала, тестируются без БД; (2) `record_answer(row)` — INSERT 15 колонок (зеркало секции 0 `db/queries.sql`, плейсхолдеры `:name`→psycopg3 `%(name)s`); (3) тонкие адаптеры `record_drill_answer`/`record_learn_answer` (эндпоинты += 1 строка). **Best-effort, НИКОГДА не ломает тренировку:** `db.get_pool()` обёрнут в try (поднятие пула, напр. `DATABASE_URL` задан, но драйвер не стоит → не 500); без `DATABASE_URL` — тихий no-op; INSERT-ошибка глотается+логируется (без секретов). **Стратегия записи — параллельная (решение пользователя, dual-write):** JSON пишется как раньше (`stats.json`/`history.json` в Drill, `srs_state/*.json` в Learn) + дополнительно журнал; чтение Stats/History пока из JSON → фронт не ломается. JSON снимем, когда чтение переедет на вьюхи. **Пак для Drill:** дыра «у `/api/drill/answer` нет имени пака» закрыта **без правки фронта** — `pack` проставляется в результат `/api/drill/hand` (`_tag` в `main.py`, резолвится через `file or _list_range_files()[0]`), а фронт эхо-возвращает весь `drill_hand` назад; `journal._stem()` снимает `.json`; отсутствующий пак (старая вкладка) → `(unknown)`, строка всё равно лагает. **`user_id`** — фиксированный dev-UUID (env `DEV_USER_ID`, дефолт `00000000-…-0001`); `answers.user_id` БЕЗ FK → строки лагают без profiles/auth-строки, реальный id придёт в D.2. **Маппинг (не-очевидное):** RFI→`villain` NULL (drill `villain_position=None`; learn `villain_position=""`); таймаут (drill) / «Показать ответ» (learn)→`action` NULL; `rating` только learn (SM-2 1/3/4); `ev` только drill — реальный из check_answer, learn EV не считает (нет загруженного range-файла в `/api/srs/answer`); `card_id` только learn; `revealed`=`req.reveal` (learn), всегда False (drill). Тесты: **`test_journal.py` (11)** — маппинг обоих тренажёров, pack-дефолт, no-op без БД, swallow ошибки пула; регрессий нет (`test_srs` 64 + `test_drill_ev` 44 зелёные в песочнице; `test_srs_api` 18 требует fastapi → локально). `.env.example` += `DEV_USER_ID` (не секрет). **НЕ сделано (осознанно, отдельные чаты):** SRS-состояние → таблица `cards` (крупный риск — `srs_state/*.json` → БД, init/next/answer/reset); чтение статистики/истории из вьюх; перенос журнала на `BackgroundTasks` (сейчас синхронно best-effort, таймаут 5с — чтобы сразу видеть строки в Supabase при проверке). **Живой коннект из песочницы не проверяется** → пользователь: `DATABASE_URL` (Session-pooler) в env → `/api/db/health` `ok:true` → ответить руку → строка в таблице `answers` (Supabase Table editor).

    **D.1-api-cards — SRS-состояние Learn → таблица `cards` (2026-06-22).** Персистентность Learn-колоды переехала с `srs_state/<stem>.srs.json` на Postgres `cards` (per-user). Новый модуль **`cards_store.py`** (соглашение «один модуль — одна задача», по образцу `journal.py`) — единственное место, знающее «БД или JSON»: 5 функций `deck_exists`/`load_deck`/`replace_deck`/`save_card`/`delete_deck`. **Математику SM-2 (`srs.py`) НЕ трогали** — менялся только слой загрузки/сохранения. **Источник правды — по конфигу (решение пользователя, вопрос «а»):** `DATABASE_URL` задан → `cards` авторитетна (читаем+пишем ТОЛЬКО Postgres, JSON-файлы заморожены); не задан → JSON как раньше (обязательная мягкая деградация — текущий прод на Railway без `DATABASE_URL` работает байт-в-байт). **Без dual-write** (в отличие от `answers`): для СОСТОЯНИЯ два источника расходятся, поэтому ровно один авторитетный. **Обработка ошибок ≠ journal:** `cards` — состояние, упавший upsert в `/answer` НЕ глотаем (всплывает 500), иначе тихо потеряли бы повтор; журнал (лог) остаётся best-effort. **`srs_api` проведён в 5 точках:** `_pack_stem` + делегаты `_deck_exists`/`_load_deck`/`_replace_deck`/`_save_card`/`_delete_deck` (эндпоинты /status /init /next /answer /summary /reset /upgrade_easy); `srs_api` сохраняет безопасный путь через `_state_path_for`→`_safe_path_in` (защита path-traversal цела) и передаёт в стор И стем-пак (ключ БД), И путь (только для JSON-ветки); удалены `_ensure_srs_dir`/`_save_deck` + неиспользуемый `from pathlib import Path`. **БД-механика (вопрос «д»):** upsert по unique `(user_id,pack,hand,position,spot,villain)` (`villain=''` для RFI); `/init` (force) = DELETE+bulk `executemany` ~1800 строк (раз на пак, дальше 409); `/answer` = один `INSERT … ON CONFLICT DO UPDATE` мутабельных SM-2-полей (НЕ переписываем всю колоду); чистые конверсии `_card_to_row`/`_row_to_card`: `next_review`/`last_seen` `'' ↔ NULL` (пустую строку держим — на ней завязаны `is_new`/`get_due_cards`), `correct_strategy` `dict ↔ jsonb` (Jsonb-обёртка в `_bind`), `stability`/`difficulty` в модели `Card` нет → пишем NULL. **`history[]` НЕ дублируется в `cards` (вопрос «в»):** SM-2 её только пишет, никогда не читает (планировщик считает `delta_days` из `last_seen`) → события живут в `answers`, `_row_to_card` ставит `history=[]`. Проверено чтением `srs.py`. **`user_id` (вопрос «е»):** переиспользуем `journal.DEV_USER_ID` (один dev-юзер на `answers` и `cards`; у `cards.user_id` FK нет → ляжет без profiles-строки) до D.2. **Миграция (вопрос «г», решение пользователя): НЕ делаем** — стартуем с чистой БД (первый `/init` в DB-режиме строит колоду как новую; старые JSON-интервалы остаются на диске, не переносятся). **Тесты:** `test_cards_store.py` (8, stdlib — JSON-паритет + round-trip конверсий + контракт стора) зелёный в песочнице вместе с `test_srs` 64 / `test_journal` 11; ядро `srs_api` (init/next/answer/reset/409/reveal/path-traversal) прогнал через shim `fastapi`/`pydantic` (PyPI в песочнице недоступен) — зелёное, доказывает проводку; **полный `test_srs_api` (18) — локально у пользователя** (нужен fastapi). **НЕ сделано (осознанно):** чтение статистики/истории из вьюх (`v_stats`/`answers`) вместо `stats.json`/`history.json`; `data/*.json` общая библиотека vs per-user (открыто). **Безопасность:** `DATABASE_URL`/пароль — только env vars, в код/чат/доки не писать. **Живой коннект из песочницы не проверяется** → пользователь: `DATABASE_URL` (Session-pooler) в env → `/api/srs/status` пусто → `/api/srs/init` → ответить карту → строка в `cards` (Supabase Table editor).

    **D.1-api-reads — чтение Drill Stats/History из журнала (2026-06-22).** `/api/stats` и `/api/history` переехали с JSON-файлов на вьюху-запрос по `answers`. Новый модуль **`stats_store.py`** (по образцу `cards_store.py`, «один модуль — одна задача», ТОЛЬКО чтение): чистые конверсии `_nest_stats`/`_history_entry`/`_fmt_ts` (тестируются без БД) + `read_stats`/`read_history`. **Источник правды симметричен `cards` (вопрос «а»):** `DATABASE_URL` задан → читаем из `answers`, иначе из `stats.json`/`history.json` как раньше (обязательная мягкая деградация — прод на Railway без переменной цел). **Drill-only (вопрос «б»):** запрос фильтрует `mode='drill'` НАПРЯМУЮ по `answers`, а НЕ через вьюху `v_stats` — `v_stats` режимо-агностична (смешала бы Learn в Drill-панель); оставлена под будущий общий/Learn-экран. Формат key совпал 1:1: `case when villain is null then position else position||'_vs_'||villain` = старые `UTG`/`BTN_vs_MP` (журнал кладёт RFI villain NULL). **Маппинг БД→фронт** (в эндпоинте/сторе): `position→hero_position`, `villain→villain_position`, `action→player_action`. **`card1/card2` в журнал не писались (вопрос «г», решение пользователя)** → лента показывает нотацию `hand` («AKs») через 1-строчный фолбэк в `drill.js` (`${e.card1 ? … : e.hand}`); масти сдачи — косметика, в `answers` не добавляем. **`ts` теперь с датой (вопрос «в», решение пользователя):** `_fmt_ts` → «ГГГГ-ММ-ДД ЧЧ:ММ» (UTC); точный per-user часовой пояс (`profiles.timezone`) отложен в чат дашборда. Таймаут: журнал хранит `action` NULL → восстанавливаем сентинел «timeout» (бейдж как в JSON-режиме). **Reset/Clear (вопрос «г», решение):** `answers` append-only — не обнуляется удалением (там же Learn-история и данные под FSRS-фит) → в DB-режиме `/api/stats/reset` и `/api/history/clear` возвращают `noop` (журнал НЕ трогаем), а `drill.js` прячет обе кнопки (узнаёт режим через `/api/db/health.configured` на загрузке). Реальный «мягкий сброс» (per-user cutoff-метка) — отдельно в чате кабинета. JSON-режим: reset/clear как раньше. **Dual-write оставлен (вопрос «д», решение):** `submit_answer` и JSON-хелперы (`load_stats`/`save_stats`/`update_stats`/`load_history`/`save_history`) НЕ тронуты — `stats.json`/`history.json` пишутся бэкапом (журнал-инсерт best-effort, файл надёжен); снимем позже, когда живой DB-путь обкатан. **На чистой БД (вопрос «е»):** статистика покажет только пост-миграционные ответы (исторических агрегатов по дням нет) — известная цена «чистого старта». **Безопасность ошибок:** DB-ошибки propagate (как `cards_store`); `loadStats`/`loadHistory` на фронте их глотают → панель просто не обновляется, без краша. Тесты: **`test_stats_store.py` (12, stdlib)** — конверсии (RFI/vs/таймаут/EV-null/ts) + JSON-ветка (паритет + clamp лимита) зелёные вместе с `test_journal` 11 / `test_srs` 64 / `test_drill_ev` 44 / `test_cards_store` 8; эндпоинт-тесты локально (нет fastapi в песочнице). Cache-bust `drill.js?v=20260622a`. **Живой DB-путь проверяет пользователь:** `DATABASE_URL` в env → ответить руку в Drill → Stats/History из `answers`, Reset/Clear скрыты.

    **D.2-auth — бэкенд + БД done (2026-06-22), фронт+Google остаются.** Изоляция данных по реальному `user_id` из JWT вместо dev-UUID. **Архитектура (решена, вопрос «а»): Supabase = только логин, данные через наш FastAPI.** Фронт через supabase-js получает JWT → FastAPI верифицирует подпись по **публичному** JWKS-ключу (асимметрия ES256/RS256 — секрета для проверки НЕ держим; Supabase-проект создан после 1.10.2025 → асимметрия по умолчанию) → `sub` = UUID юзера. Сервер ходит direct-postgres (обходит RLS) → изоляцию обеспечивает бэкенд (фильтр по user_id); RLS — бэкстоп. Альтернатива (PostgREST + RLS энфорсит) отклонена: означала бы переписать готовый бэкенд D.1 на клиент. **Гейт режима: auth активен ⟺ задан `SUPABASE_URL`** (зеркалит `DATABASE_URL`-гейт хранилища). Нет переменной → `resolve_user` отдаёт `journal.DEV_USER_ID`, логина нет, прод как раньше (мягкая деградация; вопрос «б»). **Новый `auth.py`** (один модуль — одна задача): `get_current_user` (FastAPI-зависимость), чистое ядро `resolve_user(authorization, decoder=)` (инъекция декодера → stdlib-тестируемо без fastapi/PyJWT), `extract_bearer`, `decode_claims` (PyJWT `PyJWKClient` → ES256/RS256, сверка `iss`/`aud=authenticated`/`exp`; ленивый импорт `jwt`), `AuthError`→HTTPException. fastapi-импорт защищён try/except + deferred annotations → модуль импортится и в песочнице. **Проброс `user_id`** добавлен параметром (дефолт `DEV_USER_ID` → JSON-режим и старые тесты целы) в `journal`/`cards_store`/`stats_store`; `Depends(get_current_user)` повешен на эндпоинты ДАННЫХ (`/api/drill/answer`, `/api/stats`, `/api/history`, все `/api/srs/*`). Библиотека паков (`/api/ranges*`, `/api/config`, `/api/drill/hand`) намеренно открыта — read-only, одинакова всем (вопрос «г»: паки = общая read-only библиотека). **Новый `auth_api.py`** — `/api/auth/config` отдаёт фронту `{enabled,url,anon_key}` (всё публично; `enabled`=`auth_enabled()`). `requirements.txt` += `PyJWT[crypto]`; `.env.example` += `SUPABASE_URL`+`SUPABASE_ANON_KEY` (оба НЕ секрет). **БД (вопрос «е»):** `db/supabase_d2_auth.sql` (раскомментированный аддендум) применён к живой Supabase через SQL Editor (браузером; SQL собран в-странице из коротких шаблонов — длинные base64/decimal-блобы ловят homoglyph-порчу при передаче, P-039) и **подтверждён каталогами**: триггер `handle_new_user` (`security definer`, создаёт `profiles`-строку на signup → разблокирует вьюхи дашборда) + 4 RLS-политики (`answers`/`cards`/`goals`=`own rows`, `profiles`=`own profile`, все FOR ALL `auth.uid()=user_id`/`=id`). Идемпотентно (drop-if-exists). **Миграция dev-данных НЕ делаем (вопрос «д»): чистый старт** (строки под DEV_USER_ID осиротеют, невидимы реальным юзерам). Тесты: `test_auth.py` (22 stdlib, ядро + гейтинг) + journal 11 / cards_store 8 / stats_store 12 / srs 64 / drill_ev 44 зелёные; shim-импорт `main`/`srs_api`/`auth_api` чистый (проводка `Depends` вычисляется без ошибок). **Живой JWT-путь проверяет пользователь** (нужны PyJWT + реальные токены — в песочнице нет). **НЕ сделано (отдельный чат):** фронт логина (`auth_client.js` — обёртка `fetch` + оверлей; дизайн в `roadmap.md`); включение Google-провайдера (нужен Google Cloud Console пользователя + Client Secret — НЕ через чат; шаги в `docs/auth_setup.md`); переменные окружения (включать вместе с фронтом, иначе старый UI без токена → 401); живая проверка изоляции. **Безопасность:** Client Secret / `service_role` / `DATABASE_URL` — только env, никогда в код/чат/фронт/git; anon-ключ + project ref — публичны. Детали — `roadmap.md` Track D + `docs/auth_setup.md`.

    **D.2-фронт логина done (2026-06-22; код + проверка в песочнице, живьём за пользователем).** Достроен клиент D.2: новый `static/auth_client.js` — **ПЕРВЫЙ** скрипт в `index.html`. (1) **Синхронно оборачивает `window.fetch`** и добавляет `Authorization: Bearer <JWT>` к **защищённым** `/api/*` (`drill/answer`, `stats`, `history`, `srs/*` — зеркало бэкендового `Depends(get_current_user)`, регулярка `needsAuth`). **Гейтит ТОЛЬКО защищённые**, открытую библиотеку (`ranges*`/`config`/`drill/hand`/`db/health`/`auth/config`) пропускает сразу — **почему:** `boot()` в `drill.js` тянет список паков через `fetchWithTimeout` с 5-сек abort; если бы открытые звонки ждали логина, при медленном вводе пароля (>5с) они бы оборвались и апп загрузился сломанным. Защищённые звонки ждут `authReady` (промис, резолвится при логине) и используют **plain `fetch`** (без abort) → безопасно стоят в очереди и стреляют с токеном в момент входа, наполняя панели. (2) **Жизненный цикл логина:** тянет `/api/auth/config` через НЕобёрнутый `rawFetch` (иначе дедлок); `enabled:false` → `markReady()` и больше НИЧЕГО (мягкая деградация — прод без `SUPABASE_URL` = как сегодня, ноль оверлея/supabase-js/заголовка; цена — один доп. локальный запрос); `enabled:true` → грузит **supabase-js@2 c jsDelivr динамически** (только в enabled-режиме → прод не тянет ~40KB), `createClient`, `getSession`; есть сессия → токен+`markReady`; нет → полноэкранный гейт. (3) **Гейт** (`.prx-auth-*`, токены `var(--…)` дизайн-системы, инъекция `<style>` только здесь): вордмарк PreflopRange, табы Sign in/Sign up, email+пароль, primary, «Continue with Google» (4-цвет SVG-G). `onAuthStateChange` — единственный источник свежести токена (SIGNED_IN/TOKEN_REFRESHED/SIGNED_OUT); SIGNED_OUT → `location.reload()` (чистый ре-гейт). **Sign out** — кнопка в `.topbar`. **Решения пользователя:** (а) email-подтверждение **ОСТАЁТСЯ ВКЛ** → signup без сессии → сообщение «Account created. Check your email…» + переключение на Sign in (код устойчив и к confirm-off: тогда придёт сессия и сработает onAuthStateChange); (б) **полноэкранный гейт** (не модалка — без токена данные всё равно 401, «сломанный» UI бессмысленен); (в) токен — авто-рефреш supabase-js, обёртка читает свежий; (г) `redirectTo = window.location.origin` (→ allowlist в Supabase URL Configuration). **Существующие drill/learn/visualizer/editor НЕ тронуты** — глобальная обёртка `fetch` покрывает их без правок. `index.html` += `auth_client.js?v=20260622a` первым скриптом (перед `mtx_palette.js`). **Проверка:** реальный файл прогнан в Node `vm` со стаб-DOM, 11/11: мягкая деградация (disabled → без заголовка, идентично сегодня), очередь-до-логина (открытый летит сразу, защищённый ждёт), привязка `Bearer` после входа, классификация open/protected (8+8 URL); бэкенд stdlib-тесты (auth 22 / journal 11 / cards_store 8 / stats_store 12 / srs 64 / drill_ev 44) зелёные; `/api/auth/config` смонтирован (`main.py`). **Живьём НЕ проверено** (нужны supabase-js + реальные токены + `SUPABASE_URL` — в песочнице нет) → за пользователем. **Осталось в D.2:** Google Cloud Console (руками пользователя — его Google-аккаунт, по `docs/auth_setup.md`); Google-провайдер в Supabase (Client ID — можно браузером, Client Secret вводит пользователь, НЕ через чат) + URL Configuration (Site URL + Redirect URLs вкл. `http://localhost:8000/**`); переменные `SUPABASE_URL`+`SUPABASE_ANON_KEY`(+`DATABASE_URL`) на локали/Railway — **включать ВМЕСТЕ с выкаткой фронта, иначе старый UI без токена → 401**; живая проверка (email signup/confirm/signin, Google, две учётки → изоляция `answers`/`cards`, прод без переменных — без логина). **Безопасность:** anon-ключ + project ref публичны (ушли бы во фронт через `/api/auth/config`); Client Secret / `service_role` / `DATABASE_URL` — только env.

    **D.2 live — ЗАКРЫТ (2026-06-22; чат «Track D.2 финал»).** Настройка завершена и проверена вживую на `localhost:8000`. **Supabase:** Google-провайдер уже был включён+настроен (Client ID `806350654586-…apps.googleusercontent.com` подтверждён пользователем как его; Client Secret заполнен; Callback верный — НЕ перезаписывали); URL Configuration — оба Redirect URL (`http://localhost:8000/**` + `https://prefloprange.org/**`), **Site URL временно `http://localhost:8000`** (письмо-подтверждение возвращает на локаль; ⚠ сменить на `https://prefloprange.org` при прод-выкатке — иначе письма реальных юзеров поведут на localhost); `SUPABASE_ANON_KEY` = publishable key `sb_publishable_…`; **JWT-подпись проекта = ECC P-256 (ES256)** (current key; легаси HS256 — previous) → `auth.py` (JWKS, ES256/RS256) совместим. **Локальный `.env`** (в `.gitignore`) создан: `SUPABASE_URL` + publishable key (оба публичны, вписал ассистент) + `DATABASE_URL` Session-pooler (`…@aws-1-eu-central-1.pooler.supabase.com:5432/postgres`; пароль вписал пользователь — в чат НЕ шёл; `.env` грузится через `db.load_dotenv()` при импорте `db` в `main.py`). **Живой прогон:** `/api/db/health` `ok:true,configured:true`; `/api/auth/config` `enabled:true`; auth энфорсится (`/api/stats` без токена → 401 «Missing… bearer»); регистрация email + подтверждение + вход ✅; «Continue with Google» ✅ (тем же Gmail → Supabase **связал** Google с email-юзером: тот же `sub` `53d52d54-…`, `provider:google` — identity linking, по согласию пользователя); токен (ES256, `kid` = ECC-ключа, `iss`/`aud`/`exp` верные) принят (200) ПОСЛЕ фикса сертификатов; 5 Drill-ответов записаны в `answers` под `user_id` пользователя; **изоляция проверена в БД** (SQL по `answers`: все строки помечены одним `user_id`; чтение фильтрует по нему; RLS — бэкстоп; DB-способ выбран пользователем вместо 2-го аккаунта, т.к. Google связался с тем же юзером). **Две env-ловушки (НЕ баг проекта, на Linux/Railway нет):** (1) macOS python.org-Python без CA-сертификатов → JWKS `SSL: CERTIFICATE_VERIFY_FAILED` → 401 на ВАЛИДНОМ токене (фронт пускает по наличию сессии, бэкенд режет на верификации); фикс `Install Certificates.command` — **P-040**; (2) старый uvicorn держал `:8000` → 404 на новых маршрутах, 200 на старых; фикс `lsof -ti :8000 | xargs kill -9` + рестарт — **P-041**. **Открытая опция (НЕ сделана):** захардить `certifi` в `auth.py` PyJWKClient (`ssl_context=…certifi.where()`) → независимость от сертификатов машины; отложено (env-проблема, на проде нет, трогает готовый код). **Осталось в Track D (НЕ D.2):** прод-переменные в Railway + Site URL→прод (вместе с выкаткой фронта); дашборд/цели/баннер/per-user tz/мягкий-сброс; снять JSON dual-write. **Безопасность:** `DATABASE_URL`/пароль БД/Client Secret/`service_role` — только env, в код/чат/доки/git НЕ писать; `.env` gitignored; перед `git push` — `git diff` на секреты. Детали — `docs/auth_setup.md`, `roadmap.md` Track D, `problems.md` P-040/P-041.

    **D-кабинет/дашборд Progress — фронт + слой данных done (2026-06-23; живьём за пользователем).** Кросс-сессионная per-user страница прогресса (НЕ путать с панелью Stats внутри Drill — та про текущую сессию). **Решения пользователя:** кабинет живёт в **меню пользователя справа в топбаре** (не отдельная mode-вкладка); раздел называется **Progress**; панелей четыре. **Слой данных — новый модуль `dashboard_store.py`** (соглашение «один модуль — одна задача», read-only, по образцу `stats_store.py`): `read_overview(user_id)` отдаёт ВЕСЬ дашборд одним payload'ом (один запрос с фронта). Панели: **счётчики** (тек./рекорд стрик, всего рук, выучено карт `interval_days≥21`, общая точность, Drill/Learn-сплит), **календарь-хитмап** (окно `CALENDAR_DAYS=182`), **бары** объёма+точности (день/неделя, агрегация недель на фронте из того же дневного ряда), **точность по спотам/позициям** (оба режима суммарно). Роутер **`dashboard_api.py`** `GET /api/dashboard/overview` (`Depends(auth.get_current_user)`, +1 строка `include_router` в `main.py`). **Ключевые решения схемы (важно для будущих чатов):** (1) **«День» по UTC** — `(ts at time zone 'UTC')::date`; этот литерал `'UTC'` — **единственная точка расширения** под `profiles.timezone`, когда дойдём до per-user пояса (у залогиненного `profiles.timezone` уже `'UTC'` по дефолту → переключение бесшовное). (2) **Анти-фарм встроен:** стрики (gaps-and-islands, считаются в Python — `queries.sql` 5b сам говорит «walk тривиален в коде») и флаг `trained` календаря — только по зачётным дням (≥ `session_min_hands`, дефолт 20). (3) **Запросы идут прямо по `answers`/`cards`**, порог — из `profiles` через `COALESCE(...,20)` (НЕ через вьюхи `v_daily_activity`/`v_session_days` — они INNER JOIN `profiles`, и без profiles-строки дашборд бы пустел); так дашборд работает и для dev-юзера без profiles-строки. (4) **Дашборд DB-only:** без `DATABASE_URL` → `{available:false}` + чистый экран «нужна БД». Это и есть «мягкая деградация» здесь — НЕ половинчатая реконструкция (в JSON нет дат → календарь/стрики собрать нельзя). «Выучено карт» — `count(*) filter (interval_days≥21)` по `cards` (зеркало `srs.LEARNED_THRESHOLD_DAYS`, литерал — без зависимости от движка SRS). **Фронт:** `static/account.js` — меню пользователя (аватар→дропдаун: email + Progress + Sign out), показ/скрытие страницы `#accountMode` (прячет mode-страницы; уход через любую mode-вкладку её скрывает), рендер 4 панелей чистой `buildBody(d)` (без DOM — потому тестируема); страница `#accountMode` в `index.html`; стили в `style.css` (графит+золото, Hanken; уровни хитмапа `l0–l3`, `l3`=зачётный день — «ярче»). **`auth_client.js` (аддитивные правки, bump `?v=20260623a`):** `dashboard` добавлен в `needsAuth`-regex (иначе Bearer-токен не уйдёт на защищённый `/api/dashboard` → 401); экспорт `window.PRXAuth {enabled,email,signOut}` (меню рендерит Sign out само и зовёт `PRXAuth.signOut`; дубль-кнопку гасит гард `if (#prxUserMenu) return` в `showSignOut`). Cache-bust: `style.css?v=20260623a` + `account.js?v=20260623a` (новый, последним перед `mobile.js`). **Тесты:** `test_dashboard_store.py` (11, stdlib — `_streaks`/`_window_days`/`_nest_by_spot`/`_accuracy` + деградация через monkeypatch `db.get_pool`→None, детерминированно при любом `.env`); `test_account_frontend.js` (node DOM-стаб на реальных файлах — чистые хелперы heatmap/недель, `buildBody` 3 ветки, классификация `needsAuth` open↔protected, проверка «`drill/hand` открыт, `drill/answer` защищён»). Регрессий нет (в чистой папке без `.env`: stats_store 12 / cards_store 8 / journal / srs 64 / drill_ev 44 / fsrs 32 зелёные). Превью — `static/dashboard_preview.html` (синтетика, как прочие `*_preview.html`). **НЕ сделано (осознанно, отдельные чаты):** постановка/редактирование целей + баннер-напоминание (D.6); per-user tz (`profiles.timezone`); «мягкий сброс» статистики (cutoff-метка); Learn-разрез статистики (панель спотов сейчас суммирует Drill+Learn). **Живой путь проверяет пользователь** (нужны `DATABASE_URL`+`SUPABASE_URL`+токен; в песочнице нет fastapi/psycopg). Ловушка прогона stdlib-тестов в песочнице (смонтированный `.env` + нет psycopg → ложные «красные» у sandbox-ассертов «нет БД») — `problems.md` P-042. **Безопасность:** секреты только в env vars; перед `git push` — `git diff` на секреты.

19. **Мультиформат: размеры столов (HU/8/9-max) + глубина 200bb — спроектировано 2026-06-19, ещё НЕ реализовано (Track E).** По запросу пользователя расширяем за пределы 6-max 100bb. **Охват ветки:** heads-up (2-max), 8-max, 9-max + глубина **200bb** с новым спотом **`vs_5bet`**. **Вне ветки (отложено, не передоговариваться здесь):** анте (цепляет короткий стек/спины — отдельная связка, #2 в силе), микс размеров открытия (блок `sizing`, см. #15 Ось 2), 3-max, узлы глубже vs_5bet. **Ключевой вывод аудита:** размер стола и глубина — уже свойства пака (`meta.table_size`/`stack_depth` + `config.positions`/`*_options`), и бóльшая часть кода (формат, фронт-посадка 6/8/9, `hh_parser` 2–9, `gtow_coverage`/`gtow_autoconfig`, `gtow_parse.detect_spot`) размеро-агностична → ветка это в основном **НОВЫЕ ПАКИ + точечные правки**, не переписывание движков. **Единственная структурная мина:** `gtow_parse.POSITION_ALIASES = {"HJ":"MP"}` 6-max-специфичен (в 8/9-max HJ и MP — РАЗНЫЕ места) → должен стать картой под `meta.table_size`; **до этого фикса 8/9-max-дампы НЕ импортировать** (латентный баг, `problems.md` P-037). **vs_5bet — чисто аддитивно** (зеркало vs_4bet, агрессивное действие `allin`); грань этапов: 100bb 5-бета не требует, vs_5bet включаем только на 200bb. Грейдинг остаётся бинарным на всех размерах/глубинах (#7/#8). Полная спека + node-counts (HU 4 / 6max 50 / 8max 91 / 9max 116 узлов на пак @100bb) + лестница этапов E1–E5 — **`docs/multiformat.md`**; трек — `docs/roadmap.md` Track E.

---

## Соглашения по архитектуре

- **«Один модуль — одна задача».** Новый функционал → новый файл с APIRouter → одна строка `app.include_router()` в `main.py`. Текущие роутеры: `postflop_router`, `equity_router`, `srs_router`.
- **Хардкод путей запрещён.** Используй `BASE_DIR = os.path.dirname(os.path.abspath(__file__))` и стройте пути относительно.
- **Никаких хардкод-диапазонов в коде.** Всё через `data/*.json` и `range_engine`.
- **Каждое изменение в `main.py` — минимальное.** Если добавляешь функционал, оформляй его как router в отдельном файле.
- **`equity.py` — не трогать без сильной причины.** Он проверен тремя независимыми сверками с equity.poker.
- **Порядок паков в выпадашке = по NL-лимиту, не алфавит.** `_list_range_files` сортирует через `_range_sort_key` (регулярка `NL(\d+)` → число; stake-less паки в конец). Один источник порядка для всех 4 вкладок. Имя файла (stem) — идентификатор пака для `srs_state/<stem>`/stats, поэтому переупорядочиваем сортировкой, НЕ переименованием (иначе осиротеет SRS-прогресс). P-036.

---

## Как запустить локально

```bash
cd ~/Desktop/Poker_Trainer
python3 main.py
# или для авто-перезагрузки при правках:
uvicorn main:app --reload
```

Открыть http://localhost:8000 — фронтенд там.

## Как запустить тесты

```bash
python3 test_srs.py          # 64 юнит-теста SRS
python3 test_srs_api.py      # 18 интеграционных тестов SRS API
# Аналогично для других тестовых файлов когда они появятся
```

Все тесты — обычные скрипты с `if __name__ == "__main__"`, без pytest. Падение видно по выходному коду.

---

## Открытые задачи на ближайшее

В порядке приоритета:

1. **Frontend Study Mode** — новая вкладка Learn в `index.html`, JS-страница, переименование Drill → Practice. Endpoints SRS уже готовы.
2. ~~**Данные vs_4bet**~~ — **ГОТОВО для GTOWNL10 (2026-06-17): все 15 пар залиты** (браузер-съём Chrome MCP; частоты+EV; действие `allin`/`call`). Для других паков (NL25GTOW и т.д.) — по мере надобности, метод тот же.
3. **Данные iso** — написать скрипт извлечения из `parsed/` бакетов (exploit-диапазоны против лимперов). **GTOW не источник** (подтверждено 2026-06-17: `gtow_parse.detect_spot` намеренно отказывает лимп-узлам `UnsupportedNode "limped pot"`; в `RAISE_ACTION` нет `iso`). Отдельная задача.
4. **Постфлоп: терн + ривер со сквозным сужением** на основе реальных диапазонов из распарсенной базы рук.
5. **Vulnerability** — какие карты следующей улицы роняют equity ниже цены колла.
6. **Замена DRAFT-диапазонов оппонентов** на выведенные из `parsed/` бакетов.
7. **Реплеер раздач** — фундамент Milestone 3 (анализатора). Минимальный, утилитарный.
8. **SRS Этап 1 апгрейда** (после п.1 и накопления 2–3 недель реальных ответов) — добавить `difficulty` в `Card` и логировать полную историю интервалов. Подробности и обоснование — решение #11 и `docs/ssp_mmc_ru.pdf`.

---

## Стиль работы с этим пользователем

- **Не торопись писать код.** Сначала договориться о подходе, потом писать. Большие задачи декомпозируй на этапы и предъявляй каждый отдельно.
- **Перед изменением живых файлов** (`main.py`, `equity.py`, `*_engine.py`) — попроси текущую версию, моя может быть устаревшей.
- **Не оптимизируй преждевременно.** MVP сначала, оптимизация по факту.
- **Будь честен про ограничения.** Если не уверен — скажи. Если что-то сложнее чем кажется — скажи. Если решение не оптимальное — назови это, не маскируй.
- **Тестируй то, что пишешь.** Особенно математику (equity, SM-2). Не сдавай код без прогона тестов.
- **Объясняй.** Пользователь учится — пара предложений «почему так» важнее краткости.

---

## Как вести работу по чатам (Cowork)

Модель работы: **один чат — одна дискретная задача/подзадача.** Между чатами история разговора НЕ переносится — переносятся только файлы на диске. Поэтому документы ниже это механизм передачи состояния между чатами, а не бюрократия.

**Карта документов (что где искать):**
- `CLAUDE.md` (этот файл) — архитектура, принятые решения, конвенции. Автозагружается в каждый чат.
- `docs/roadmap.md` — четыре параллельных трека (A/B/C/D), их статус и приоритеты. **Кросс-чатовое отслеживание задач живёт здесь**, не в task-листе (task-лист эфемерный, живёт только внутри одного чата).
- `docs/problems.md` — журнал багов: проблема → решение → регрессии. Записи P-NNN.
- `docs/design.md` — дизайн-система: направление (GG-стол + GTOW-строгость), токены, инвентарь компонентов.
- `docs/tracks_board.md` — визуальная доска всех треков и задач (Mermaid-блок-схемы, рендерятся на GitHub). Вид сверху для `roadmap.md`; статусы обновлять вместе с ним.
- `docs/budget.md` — бюджет проекта: журнал фактических затрат на разработку (подписки-инструменты: Claude, GTO Wizard, Higgsfield, домен) + сводка будущих расходов/доходов со ссылками на Track D и `icp_and_pricing.md`. Обновлять при каждой новой оплате/продлении.

**Начало чата.** Пользователь называет задачу и трек одной строкой (например «Track D.1.1 — поднять Postgres + seed»). Claude первым делом читает релевантные доки: нужный трек в `roadmap.md`, связанные баги в `problems.md`, для визуальных задач — `design.md`. `CLAUDE.md` уже загружен.

**Конец чата (definition of done).** Обновить документы под реальность: статус подзадачи в `roadmap.md`, новые/закрытые баги в `problems.md`, новые архитектурные решения здесь в `CLAUDE.md`. Без этого следующий чат не узнает что произошло. Claude делает это сам в конце задачи.

**Когда новый чат, когда тот же.** Новый — на новый трек или крупную подзадачу, либо когда текущий чат стал длинным а задача закрыта. Тот же — для тесно связанных шагов в одну посадку и быстрых follow-up правок к только что сделанному.

**Раскладка треков на чаты:** Track B — каждая подзадача (фишки / анимации / матрица / мобилка) свой чат, не смешивать. Track D — каждый шаг D.1.x свой чат, они крупные. Track A.2 (практика) — без чата. Track C — мелочь, пристёгивается хвостом.
